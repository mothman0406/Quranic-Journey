# NoorPath / Quranic Journey — Status & Next Steps

_Last updated: May 4, 2026 (Slice 2L.0.nn.c Test Mushaf audio word highlighting Phase 1c is committed at `ac56040` and all four refs are synced.)_

---

## Documentation rule for every new action

After every meaningful action, update this file and `PHASE_2D_HANDOFF.md` before handing off:

- Update the "Last updated" line with the date and actual current state.
- Record the active branch, latest local SHA, and whether remote branches are synced or stale.
- Move completed work out of "next" sections and into the done/current-state section.
- Add any hardware QA results or known failures immediately.
- Make the next action checklist concrete enough that a fresh Codex/Claude chat can start without reconstructing context.

## Current work log — April 29-30, 2026

- Active branch/SHA at Phase 2H.1 start: `main` at `21535d4`. Phase 2H.1 mobile onboarding/profile management implementation commit: `5af7b9a`; docs sync: `c7c0a65`; follow-up onboarding polish commit: `3f43337`; current docs-sync HEAD contains this note.
- Remote sync status after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` should be synced to the current Phase 2H.1 complete HEAD. `safe-cumulative` was temporary archaeology and can be ignored.
- Active branch/SHA at Phase 2H.2 start: `main` at `7e1a24e`. Phase 2H.2 dashboard parity content implementation commit: `ab87771`. Mohammad hardware-tested the dashboard after the commit and reported it looks good. This docs update should be committed/synced as the next HEAD. Remote sync target after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.2 complete HEAD.
- Active branch/SHA at Phase 2H.3 start: `main` at `512d03c`. Phase 2H.3 settings/targets convergence is implemented locally in the active Expo app. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.3 local-validation HEAD.
- Active branch/SHA after Phase 2H.3 implementation sync: `main` at `4f85482`. Mohammad hardware-tested Phase 2H.3 settings/targets convergence and reported it looks good. This docs update should be committed/synced as the next HEAD. Remote sync target after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.3 complete HEAD.
- Active branch/SHA at Phase 2H.4 start: `main` at `5872c58`. Phase 2H.4 review essentials parity is implemented locally in the active Expo app. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.4 local-validation HEAD.
- Active branch/SHA after Phase 2H.4 implementation sync: `main` at `8873156`. Mohammad hardware-tested Phase 2H.4 review essentials parity and reported QA testing is good. This docs update should be committed/synced as the next HEAD. Remote sync target after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.4 complete HEAD.
- Active branch/SHA at Phase 2H.5 start: `main` at `24abba7`. Phase 2H.5 reading essentials parity is implemented locally in the active Expo app. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.5 local-validation HEAD.
- Active branch/SHA after Phase 2H.5 implementation sync: `main` at `4ceab5b`. Mohammad hardware-tested Phase 2H.5 reading essentials parity and reported it looks good. This docs update should be committed/synced as the next HEAD. Remote sync target after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.5 complete HEAD.
- Active branch/SHA at Phase 2H.6 start: `main` at `1a08ec2`. Phase 2H.6 memorization discovery parity is implemented locally in the active Expo app. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.6 local-validation HEAD.
- Active branch/SHA after Phase 2H.6 implementation sync: `main` at `1d187ad`. Mohammad hardware-tested Phase 2H.6 memorization discovery and reported QA passed. This docs update should be committed/synced as the next HEAD. Remote sync target after this docs sync: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2H.6 complete HEAD.
- Phase 2H.6 follow-up clarification: Mohammad confirmed QA passed but the beta is not done. The old "Phase 2K/TestFlight readiness next" plan was too early. Before TestFlight, mobile must close specific web-app parity gaps in Memorization, Review, and Full Quran/Mushaf. Phase 2I/2J/2K are now pre-TestFlight blockers, not optional content-depth work.
- Active branch/SHA at Phase 2I.1 start: `main` at `561910d`. Phase 2I.1 memorization overview cards are implemented locally in the active Expo app. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.1 local-validation HEAD.
- Active branch/SHA at Phase 2I.1 save-semantics follow-up start: `main` at `2d97b71`. The follow-up is implemented locally in the active Expo app; remote sync target after this implementation/docs commit is `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.1 save-semantics local-validation HEAD.
- Active branch/SHA at Phase 2I.1 cumulative schedule QA fix start: `main` at `7006124`. Mohammad found during hardware QA that the backend cumulative recitation rhythm was wrong. The scheduler now follows the required cadence: four new-work days, one block cumulative recitation for those four days; after two such blocks, one full cumulative recitation; the final full-surah test is always a separate final day; and a partial cumulative day is inserted before the final test only when 2+ leftover new-work sessions happened since the last cumulative review. Local validation passed with a focused scheduler smoke check, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Remote sync target after this fix/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.1 cumulative-schedule local-validation HEAD.
- Active branch/SHA at Phase 2I.1 moving-current-work QA fix start: `main` at `da5ae17`. Mohammad clarified that if today's assignment contains multiple surahs/ranges, all of them are `Today's work`; `Current work` should keep advancing to the next unfinished item inside that daily assignment and should not show `Complete` until the entire daily assignment is complete. Mobile save logic now keeps multi-surah daily progress `in_progress` until every surah in today's target range is memorized, and the `Current work` card only uses the daily `memStatus === "completed"` state for its completed UI. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this fix/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.1 moving-current-work local-validation HEAD.
- Phase 2I parity audit Apr 29: re-read `artifacts/noor-path/src/pages/quran-memorize.tsx` and `artifacts/noor-path/src/pages/memorization.tsx` against the current mobile `app/child/[childId]/memorization.tsx`. Identified additional web-only behaviors mobile still lacks beyond what the original 2I.2/2I.3 rows captured: (a) Pause & Save modal lets the parent pick the actual completed-to ayah before routing to recitation check, not just the current session range; (b) Save & Leave / Leave without saving / Keep practicing modal so leaving mid-session is an explicit choice instead of a discarded session; (c) a separate `phase: "check"` recitation check screen with score card for "Recite to NoorPath" runs and Needs Work / Good / Excellent rating, distinct from the current Mark-Complete-only entry point; (d) "Ready to Recite?" choice modal at session end that routes to teacher-check or self-recite; (e) per-session bookmark with current ayah, repeat count, autoAdvance, cumulativeReview, reviewRepeatCount so a session can resume mid-range from a "Resume where you left off" card; (f) View in Full Mushaf action from inside the session; (g) a separate session setup phase before playback starts with From/To inputs, repeat slider, auto-advance toggle, cumulative review toggle, review repeat count stepper, and surah navigation; (h) "Just Get Tested" shortcut for review-only sessions that skips memorization and routes straight to the recitation check; (i) Skip repeat and Skip ayah controls in the audio toolbar so the kid can move on without finishing all repeats; (j) confetti / celebration overlay for surah-complete and session-complete instead of a plain Alert; (k) tappable ayah end-marker that opens an ayah sheet with actions for that ayah; (l) word translation tooltip on tap (with a WBW audio play button using `audio.qurancdn.com/wbw/...`) in addition to the current long-press popup; (m) auto-flip mushaf page when recite mode crosses a page boundary; (n) Show Word and Skip Word floating affordances during recite mode for stuck words; (o) page navigation pills + "Current word →" link during multi-page recite sessions; (p) cumulative review / review repeat count in persisted memorization defaults via `useSettings`-style persistence (currently those two reset to off / 3 every session in mobile); (q) per-surah Tajweed Notes accordion (`surah.tajweedNotes`) on the discovery page; (r) red / orange / green review tone (`getReviewStrengthTone`) on each surah row driven by ayah-level strength, in addition to the existing status pill. These items are added below as new 2I.2/2I.3/2I.4 sub-rows; nothing was removed from the existing 2I.2/2I.3 rows.
- Hardware bugs found Apr 29: tapping the `Next up` card on the Memorize page sometimes landed on a screen that did not load (loading death spiral / circle of death); and on the Memorize page the surah search keyboard did not dismiss on scroll and had no Done / dismiss affordance, so it covered half the screen. Both bugs are now hardware-tested/shipped by Mohammad; temporary Bug B diagnostics have been removed. The resumed Phase 2I.1 hardware QA pass also passed.
- Active branch/SHA at Memorize search keyboard Bug A fix start: `main` at `97c62e5`. `feature/main-working-branch` was fast-forwarded to `main` before this work, so `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were all at `97c62e5`. The active mobile `MemorizationDiscovery` scroll surface now dismisses the iOS keyboard on drag, lets taps on filters/rows be handled while the keyboard is up, and makes the search return key dismiss the keyboard. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this fix/docs commit: all four refs synced to the final Bug A fix HEAD. Mohammad should hardware-test this keyboard fix in isolation before Bug B work starts.
- Bug A hardware QA result: Mohammad hardware-tested the Memorize search keyboard fix after commit `6a9e70f` and reported it is shipped.
- Bug B hardware QA/diagnostic cleanup result: Mohammad confirmed the Memorize `Next up` loading death spiral is fixed. Cleanup started on `main` at `4ce3585`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. The temporary Noor memorization diagnostic console logs were removed from `artifacts/noor-mobile/app/child/[childId]/memorization.tsx` with no behavior changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this cleanup/docs commit: all four refs synced to the final Bug B cleanup HEAD.
- Bug C hardware QA result: Mohammad hardware-tested the Needs Work → Practice same-target loader fix after commit `a94c541` and reported it worked. Root cause was `beginSession` clearing loaded verse/timing maps and setting `loading` while the new practice target reused the same surah/range/reciter values, so the verse/timing fetch effect did not re-run. Mobile now increments a `sessionLoadId` on every explicit session start and gates the fetch effect on `sessionRequested`, so same-target practice sessions reload cleanly. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this docs commit: all four refs synced to the final Bug C hardware-confirmed docs HEAD.
- Phase 2I.1 full hardware QA result: Mohammad completed the full 2I.1 checklist on iPhone after commit `ddc44b6` and reported it is done. The pass covered opening Memorize from the dashboard, confirming `Today's work` / `Current work` or `Recitation Focus` / `Next up`, rated save with Needs Work / Good / Excellent, frozen daily assignment behavior with done/in-progress status, moving current work within the daily assignment, red/yellow/green ayah strength display, extra work changing `Next up` without disturbing today/current, dashboard refresh, search/filter/resume/session smoke, and Needs Work -> Practice loading. Active branch at Phase 2I.2a start: `main` at `ddc44b6`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced.
- Active branch/SHA at Phase 2I.2a implementation start: `main` at `ddc44b6`, with all four refs synced. Phase 2I.2a is implemented locally in the active Expo app: the fixed bottom session action now opens a Pause & Save sheet; normal memorization sessions let the parent choose the completed-to ayah with a bounded stepper plus Current ayah / Full range shortcuts; review-only assignments show an all-or-nothing Finish Recitation path with a `Go to Recitation Check` CTA; the existing Needs Work / Good / Excellent rating sheet now rates and persists only ayahs through the selected completed-to value; and today's daily progress uses that selected completed-to ayah so partial saves remain `in_progress`. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.2a local-validation HEAD.
- Phase 2I.2a hardware QA result: Mohammad hardware-tested the Pause & Save completed-to picker after commit `1982baa` and reported QA passed. The pass covered normal memorization Pause & Save with a smaller completed-to ayah, rating/save with Needs Work / Good / Excellent, partial-save daily progress staying `in_progress`, full-range save behavior, review-only `Finish Recitation` -> `Go to Recitation Check`, `Keep Practicing` closing without saving, and the search/filter/resume/session smoke including Needs Work -> Practice loading. Remote sync target after this docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.2a hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2b implementation start: `main` at `1fe3b78`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `1fe3b7836bcc5c8c6b3f5b87e09435e965f2cc5b`. Phase 2I.2b is implemented locally in the active Expo app: active-session Back opens a leave confirmation sheet with `Save & Leave`, `Leave without saving`, and `Keep Practicing`; React Navigation `usePreventRemove` catches active route-removal/back gestures where Expo Router provides `beforeRemove`; `Save & Leave` routes into the existing 2I.2a Pause & Save completed-to/rating flow; review-only sessions remain all-or-nothing; `Leave without saving` stops speech recognition/audio, unloads sound, clears active session state, returns to the Memorize overview, and does not call memorization or daily-progress POSTs; `Keep Practicing` simply dismisses the sheet so audio/session state stays intact where possible. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after validation/docs commit: all four refs synced to the final Phase 2I.2b local-validation HEAD.
- Phase 2I.2b hardware QA result: Mohammad hardware-tested the Save & Leave / Leave without saving / Keep Practicing modal after commit `e134234` and reported QA passed. The pass covered the active-session Back leave sheet, `Keep Practicing`, normal-session `Save & Leave` through the completed-to picker and rating flow, review-only all-or-nothing `Save & Leave`, `Leave without saving` returning to Memorize without saving progress, and an iOS active-session back/abort smoke check. Remote sync target after this docs commit: `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to the final Phase 2I.2b hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2c implementation start: `main` at `1b72b03`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `1b72b036d075a13d366b33b97994cdefd314e998`. Phase 2I.2c is implemented locally in the active Expo app: the old bottom rating sheet is now a full-screen Recitation Check surface; Pause & Save / Save & Leave still route through completed-to selection first; review-only assignments remain all-or-nothing; session-complete now opens Recitation Check with the full range selected; recite-mode completion opens Recitation Check with a Recite to NoorPath score card and the existing Needs Work / Good / Excellent quality values `2 / 4 / 5`. No frozen web files, generated files, backend/spec files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after validation/docs commit: all four refs synced to the final Phase 2I.2c local-validation HEAD.
- Phase 2I.2c hardware QA result: Mohammad hardware-tested the Recitation Check phase after commit `6d85d59` and reported QA passed. This clears the full-screen Recitation Check surface, existing 2I.2a completed-to save semantics, 2I.2b leave semantics, review-only all-or-nothing path, and Recite to NoorPath score-card path for the next slice. Remote sync target after this docs commit: all four refs synced to the final Phase 2I.2c hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2d implementation start: `main` at `94f5602`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `94f56029ca5fc5697b07ffbe5d7f2d90152331bb`. Phase 2I.2d is implemented in the active Expo app with implementation commit `0fc6a1d`: normal session-complete now opens a `Ready to Recite?` sheet after the final ayah/repeat/cumulative review finishes; `Recite to Teacher` opens the existing Recitation Check with source `teacher`; `Recite to NoorPath` restarts existing recite mode from the assigned first ayah and returns to Recitation Check with the existing score card on completion. Existing Pause & Save, Save & Leave, Leave without saving, review-only all-or-nothing saves, daily-progress posting, recite matcher behavior, tajweed backlog status, frozen web files, generated files, backend/spec files, and native dependencies were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to docs commit `2545acd` before the hardware-QA docs update.
- Phase 2I.2d hardware QA result: Mohammad hardware-tested the Ready to Recite modal after docs commit `2545acd` and reported QA passed. The pass clears normal session-complete -> `Ready to Recite?`, `Recite to Teacher` -> Recitation Check source `teacher`, `Recite to NoorPath` -> existing recite mode -> score-card Recitation Check, and the smoke check for existing 2I.2a/2I.2b/2I.2c save/leave/check behavior. Remote sync target after this docs commit: all four refs synced to the final Phase 2I.2d hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2e implementation start: `main` at `149d83a`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `149d83aa419d0c26330587d58072f1d38eb70ee4`. Phase 2I.2e is implemented locally in the active Expo app: review-only overview assignments now show a `Just Get Tested` shortcut, and tapping it loads the full review-only assignment range directly into the existing Recitation Check surface with source `teacher`, without starting memorization playback. Existing review-only all-or-nothing saves, normal-session completed-to saves, leave semantics, Recitation Check behavior, Ready to Recite for normal session-complete, dashboard/overview entry behavior, ayah/page modes, audio highlighting, cumulative review, reciter settings, playback speed, blind/blur modes, translation popup, recite mode, mark-complete persistence, tajweed backlog status, frozen web files, generated files, backend/spec files, and native dependencies were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2I.2e local-validation HEAD.
- Phase 2I.2e hardware QA result: Mohammad confirmed the `Just Get Tested` button works after implementation commit `e654ceb`. At the start of the follow-up skip-gap fix, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were all synced to `e654ceb4e66cc0f3b712d38a1d1480dddce8dda7`.
- Next up skip-gap fix status: implemented in API commit `3f354f4` after Mohammad reported finishing ayahs 1-16 and seeing `Next up` jump to ayahs 50-76. Root cause: dashboard/up-next fallbacks still treated `versesMemorized` as a contiguous prefix and some same-day daily-progress paths preferred the newest duplicate row. The fix uses explicit contiguous `memorizedAyahs` to choose the next new ayah, treats a surah as fully memorized only when the contiguous prefix reaches the final ayah, prefers the earliest matching workflow target for today's dashboard row, reuses an existing same-day daily-progress row instead of inserting duplicate memorization rows, and makes `/memorization` auto daily-progress updates choose the row that overlaps the just-saved session. Local validation passed with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. No frozen web files, generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `3f354f4d82f81527149bb729ee5b1e9b2e4cccc5`; after this docs commit, sync all four refs to the final docs HEAD.
- QA status: Phase 2D memorization core through Slice 5b, Phase 2E dashboard polish, Phase 2F target-setting UI, Phase 2G.1 diagnostic cleanup, Phase 2G.2 mobile IA shell, Phase 2G.3 shared screen primitives, Phase 2H.1 mobile onboarding/profile management, Phase 2H.2 mobile dashboard parity content, Phase 2H.3 settings/targets convergence, Phase 2H.4 review essentials parity, Phase 2H.5 reading essentials parity, Phase 2H.6 memorization discovery parity, Phase 2I.1 overview cards/save semantics, Phase 2I.2a Pause & Save, Phase 2I.2b Save & Leave, Phase 2I.2c Recitation Check, Phase 2I.2d Ready to Recite, Phase 2I.2e Just Get Tested, Phase 2I.2f Pre-session setup, Phase 2I.2g Bookmark / resume, Phase 2I.2h View in Full Mushaf, Phase 2I.2i Skip repeat / Skip ayah, Phase 2I.2j Tappable ayah end-marker, Phase 2I.2k tap-tooltip translation + WBW audio, Phase 2I.2l recite page auto-flip, Phase 2I.2m Show Word / Skip Word plus real blur, Phase 2I.2n multi-page recite navigation pills, Phase 2I.2o confetti / celebration overlay, Phase 2I.3 memorization settings/defaults, the `Next up` skip-gap fix, the Today's/Current work freeze correction, and the Apr 29 Memorize Bug A/B/C fixes are hardware-tested. Phase 2G.4 local validation passed for API/codegen/mobile with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-spec run codegen`, `/Users/mothmanaurascape.ai/Library/pnpm/pnpm run typecheck:libs`, `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, and `cd artifacts/noor-mobile && npx tsc --noEmit`. Phase 2H.1 through 2H.6 local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Phase 2I.1/2I.2/2I.3 local validation passed with focused API typecheck where needed, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Full root `/Users/mothmanaurascape.ai/Library/pnpm/pnpm run typecheck` was previously attempted, but it stops in unrelated frozen/reference UI areas (`artifacts/noor-path/src/components/ui/button-group.tsx`, `artifacts/noor-path/src/components/ui/calendar.tsx`, `artifacts/mockup-sandbox/src/components/ui/calendar.tsx`, `artifacts/mockup-sandbox/src/components/ui/spinner.tsx`) on React type/ref baseline errors.
- Dev-server note: starting Expo inside the sandbox fails with `ERR_SOCKET_BAD_PORT` because sandboxed Node cannot bind local ports (`EPERM` on 8081). Run the dev server outside the sandbox/escalated when using this environment.
- TestFlight status: deferred. Phase 2I Memorization parity completion is hardware-tested through 2I.4 and Phase 2J.1-2J.3 are hardware-tested. Do not start TestFlight readiness until the remaining pre-TestFlight parity blocker is complete and hardware-tested: Phase 2K Full Quran/Mushaf Bayaan parity.
- Inspection notes: initial Phase 2E inspection found `app/child/[childId]/index.tsx` was a three-card skeleton; `src/lib/api.ts` is a thin authenticated fetch helper; `/api/children/:id/dashboard` exposes `todaysPlan.newMemorization`, `todayProgress`, `reviewsDueToday`, and `readingGoal`; `/api/children/:id/reviews` exposes detailed queue items with `reviewPriority`.
- Implementation notes: mobile dashboard now fetches dashboard plus review queue data, the Memorization/Review/Reading cards show today's assigned work, review previews use shared red/orange/green priority styling, the review queue cards have matching priority rails/backgrounds, and the profile selector has richer child rows with age, streak, and points.
- Diff-review notes: removed new dashboard letter spacing and fixed streak pluralization before final QA.
- Phase 2F inspection notes: targets are stored on `children` as `memorizePagePerDay`, `reviewPagesPerDay`, and `readPagesPerDay`; `GET /api/children/:childId` returns them through `formatChild`; `PUT /api/children/:childId` accepts all three fields. Web reference options live in `artifacts/noor-path/src/pages/settings.tsx`.
- Phase 2F implementation notes: added `app/child/[childId]/targets.tsx`, registered it in the child stack, added a dashboard `Targets` entry point, and made the dashboard refresh on focus after returning from target edits. The screen uses preset chips plus minus/plus fine tuning and saves directly through `apiFetch`.
- Phase 2F hotfix notes: after screenshots showed the dashboard rendering API 500s for L and Joll, production was checked directly and returned 200 for both children. `apiFetch` now sends `x-local-date` from the phone and normalizes JSON/plain-text/HTML failures into short readable error messages instead of showing full HTML documents. The dashboard now retries `/dashboard` once and then falls back to child/profile plus review queue data, keeping Targets reachable if today's plan endpoint flakes. Fresh inspection found that if the fallback path itself fails, the full-screen error still only shows the original dashboard error, so the phone cannot distinguish stale JS, wrong API base, dashboard failure, review failure, or child fallback failure yet.
- Phase 2F diagnostic implementation notes: added `ApiError`, `getApiRuntimeInfo`, console request/response logs, and diagnostic marker `dashboard-diag-2026-04-28a` in mobile `apiFetch`. The dashboard now tracks primary/retry/fallback review/fallback child stages, renders a compact diagnostic panel on fallback/error, and if the child fallback fetch fails it renders a degraded shell from route params instead of the original full-screen dashboard 500.
- Phase 2F route bug diagnosis: dashboard `handleTargetsPress` used relative `pathname: "./targets"`, which Expo Router resolved as `/child/targets` instead of `/child/:childId/targets`. That made the dashboard route match `[childId] = "targets"` on return/reload, causing API calls to `/api/children/targets/*`.
- Phase 2F route fix notes: dashboard Targets navigation now uses absolute `pathname: "/child/[childId]/targets"`, and dashboard loading now rejects non-numeric child IDs before making API calls.
- Phase 2F hardware QA notes: Mohammad reported the QA check passed after the route fix. The child dashboard no longer gets stuck on `/api/children/targets/*`, and the Targets/dashboard flow is unblocked.
- Phase 2G.1 implementation notes: removed the temporary visible dashboard diagnostic panel, deleted the `[noor-api]` request/response/error console logs, and removed the temporary dashboard diagnostic marker/runtime helper. Preserved `ApiError`, readable API errors, `x-local-date`, Better Auth cookie handling, dashboard retry/fallback behavior, degraded fallback shell, and numeric route-param validation.
- Phase 2G.1 hardware QA notes: Mohammad reported dashboard load/refresh, Targets navigation, and fallback behavior work after the cleanup.
- Phase 2G.2 implementation notes: added a reusable `ChildBottomNav`, registered a new child `more` route, added bottom-nav access on dashboard and review, added a review-count badge where queue data is already loaded, and built a More screen exposing Full Quran, Targets, Profiles, plus planned Progress, Learning Plan, Stories, and Du'aas entries. Kept the memorization engine, review session, Mushaf controls, web app, tajweed, generated files, and native dependencies untouched.
- Phase 2G.2 hardware QA notes: Mohammad reported the bottom nav and More screen passed on iPhone.
- Phase 2G.3 implementation notes: added shared screen primitives in `src/components/screen-primitives.tsx` (`ScreenContainer`, `ScreenHeader`, `ScreenScrollView`, loading/empty/error states, inline error, section label, card group, badge pill, and list row). Adopted them on `more.tsx` and `review.tsx` only, keeping behavior and routes unchanged. Memorization, review session, Mushaf controls, tajweed, generated files, web app, and native dependencies were untouched.
- Phase 2G.3 hardware QA notes: Mohammad reported the shared screen primitives QA pass completed successfully.
- Phase 2G.4 implementation notes: fixed child du'a status updates to match by both `childId` and `duaId`, taught `POST /api/children` to honor `readPagesPerDay`, expanded `lib/api-spec/openapi.yaml` for child targets/create/delete/goals, dashboard `todayProgress`/`readingGoal`/`upNextMemorization`, reviews `todayRange`/`reviewedToday`, daily/reading/weekly progress, ayah strengths, and rated ayahs, then regenerated Orval outputs. Also resolved the non-generated `@workspace/api-zod` barrel export collision caused by regenerated Zod schemas without editing generated files manually.
- Phase 2H.1 implementation notes: added mobile child creation from the profile picker, a shared mobile child profile form, prior-memorized surah selection with per-surah known ayah counts and initial strength, daily targets (`memorizePagePerDay`, `reviewPagesPerDay`, `readPagesPerDay`), `practiceMinutesPerDay`, Stories/Du'aas visibility toggles, an existing-child Profile Settings route from More, and a destructive delete flow with confirmation that targets only the selected child. The mobile API helper now handles `204 No Content` responses so `DELETE /api/children/:childId` works cleanly. Follow-up polish after comparing the frozen web add-profile flow added From/To surah range selection, an always-reachable sticky Save button, keyboard drag dismissal, and an iOS Done accessory so name/age inputs no longer trap the keyboard over the form. No generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.1 hardware QA notes: Mohammad tested the onboarding/profile management flow after the range/sticky-save/keyboard polish and reported it looks good.
- Phase 2H.2 implementation notes: expanded the active mobile dashboard with API-backed memorization stats, goals preview, achievements preview, story and du'a suggestions that respect `hideStories`/`hideDuas`, up-next memorization preview from `upNextMemorization`/`nextSurah`, richer completed and empty states for memorization/review/reading, and quick actions for Targets, Full Quran, Profile, and More. Kept the work frontend-only in `artifacts/noor-mobile/app/child/[childId]/index.tsx`; no backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.2 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2H.2 hardware QA notes: Mohammad tested the enriched dashboard and reported it looks good.
- Phase 2H.3 implementation notes: converted mobile `app/child/[childId]/targets.tsx` into a parent Settings surface while preserving the existing route. It now includes profile entry, practice minutes presets/stepper, daily target bundles plus individual memorize/review/read controls, content visibility toggles for `hideStories` and `hideDuas`, and JS-only memorization session defaults for repeat count, auto-advance delay, page-range autoplay, blind mode, and blur mode. `src/lib/settings.ts` now persists those defaults in AsyncStorage per child, and `memorization.tsx` hydrates them as starting values without changing the memorization engine or recite matcher. Profile-level memorization preferences now save immediately instead of via a debounce that could be lost on fast navigation. Dashboard/More labels now point users to Settings. No backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.3 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2H.3 hardware QA notes: Mohammad tested the settings/targets convergence on iPhone and reported it looks good.
- Phase 2H.4 implementation notes: refreshed the active mobile Review screen with focus-refresh and pull-to-refresh, a dense queue summary, richer due-today cards, reviewed-today completed cards, no-due/completed/empty states, and an upcoming reviews section capped with overflow copy. The review session keeps the page-image review flow and 0-5 SM-2 submit path, adds a sticky bottom control bar, hydrates the child's memorization reciter preference as the review audio default, and adds JS-only reciter/playback-speed controls using existing mobile audio patterns. Submitting a rating returns to Review, whose focus refresh updates the queue; dashboard focus refresh continues to update the badge/count after returning. No backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.4 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2H.4 hardware QA notes: Mohammad tested the review essentials parity on iPhone and reported QA testing is good.
- Phase 2H.5 implementation notes: upgraded the active mobile Full Quran reader while preserving the streamed 604 page images, RTL FlatList paging, last-page resume, manual Save, debounced auto-save, and fixed page shortcuts. The reader now has local Mushaf page/surah/juz metadata, a jump sheet for page/surah/juz search, compact current-surah and saved-resume clarity, visible reading target progress with counted-today copy, previous/next controls, and JS-only per-child page bookmarks stored in AsyncStorage. No backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.5 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2H.5 hardware QA notes: Mohammad tested the reading essentials parity on iPhone and reported it looks good.
- Phase 2H.6 implementation notes: added a mobile memorization discovery surface at the existing Memorize route. It loads dashboard current work, in-progress resume items, `/memorization` progress, and `/surahs`; provides search/filter chips; shows per-surah status, progress, ayah counts, strength dots, and compact ayah strength strips; and starts today's assignment, resume work, or a selected surah by entering the existing memorization session engine. The session engine remains in the same file and preserves ayah/page modes, audio highlighting, cumulative review, reciter settings, playback speed, blind/blur modes, translation popup, recite mode, and mark-complete persistence. `src/lib/quran.ts` now requests optional `page_number` so selected-surah sessions can derive exact page ranges when the Quran.com response provides them, with a local Mushaf fallback estimate. No backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2H.6 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2H.6 hardware QA notes: Mohammad tested memorization discovery on iPhone and reported QA passed.
- Phase 2H.6 correction/research notes: frozen web reference still has required mobile parity not covered by 2H.6. `artifacts/noor-path/src/pages/memorization.tsx` has a top three-card row for `Today's Work`, `Current Work`/`Recitation Focus`, and `Next Up`, wired to dashboard `todaysPlan.newMemorization`, `todayProgress`, and `upNextMemorization`. `artifacts/noor-path/src/pages/quran-memorize.tsx` has setup/session/check flow with ayah From/To, repeat count, auto-advance, cumulative review, review repeat count, bookmark resume, Pause & Save, Save & Leave, Leave without saving, Recite to Teacher/NoorPath, and View in Full Mushaf. `artifacts/noor-path/src/pages/settings.tsx` and `src/hooks/use-settings.ts` define global defaults for confetti, auto-advance, default repeat count, cumulative review, default review repeat count, blur intensity, reciter, Mushaf theme, and font size. `artifacts/noor-path/src/pages/review.tsx` has local active-day review behavior, completed-today/completed-day sections, and a Continue Reviewing path into the next open review set so the user is not stuck with nothing to review. `artifacts/noor-path/src/pages/mushaf-reader.tsx` is a Bayaan-style Mushaf tool with fitted text pages, surah/bookmark search, recent reads, bookmarks, highlights, notes, translation/tafseer/word-by-word sheets, audio range playback, reciter/rate/repeat/range-repeat settings, blind/select/recite modes, mark-selected-ayahs-memorized, memorization handoffs, and reading-progress persistence.
- Phase 2I.1 implementation notes: added the web-style top three Memorize overview cards in the active mobile app: `Today's work`, `Current work`/`Recitation Focus`, and `Next up`. The cards read from dashboard `todaysPlan.newMemorization`, `todayProgress.memStatus`, and `upNextMemorization`; show understandable work labels, ayah ranges, page ranges, status, and actions; and route into the existing mobile memorization session engine. `Today's work` starts the full dashboard assignment range, while `Current work`/`Recitation Focus` starts the active current-work range. Dashboard Memorize entry behavior remains the existing overview route. No backend/spec/generated files, frozen web app files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2I.1 validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally. Full hardware QA passed on Apr 29, 2026.
- Phase 2I.1 follow-up/save-semantics implementation notes: mobile `memorization.tsx` now opens a rating sheet before final save with `Needs Work` quality 2, `Good` quality 4, and `Excellent` quality 5. Saving fetches current memorization progress, unions the session ayahs with existing memorized ayahs, rates only the current session ayahs, and sets status to `"memorized"` only when the full surah is complete; otherwise it saves `"in_progress"`. For today's assigned memorization/recitation target, mobile posts `/api/children/:childId/daily-progress` with completed/in-progress status, `memCompletedAyahEnd`, and target fields so the same `Today's work` card stays visible with saved status while `Next up` can change after extra work. The flow returns to the Memorize overview and refreshes dashboard/progress/surah data. `src/lib/memorization.ts` now allows `"in_progress"` saves and exposes a `submitDailyProgress` helper. No frozen web app files, generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2I.1 current-work status follow-up: the `Current work`/`Recitation Focus` overview card now treats current work as a moving pointer within the full daily assignment. If today's assignment spans multiple surahs/ranges, the card should advance to the next unfinished item as progress is saved and should not show `Complete` until all of `Today's work` is complete. The green completed card tone/action only appears when daily progress is completed.
- Intra-surah review clarification/fix: `buildSurahMemorizationWorkflow` now splits a long surah into page-target chunks and schedules cumulative recitation with the cadence Mohammad requested during Phase 2I.1 QA. The pattern is: Day 1-4 new work, Day 5 cumulative recitation of Days 1-4; Day 6-9 new work, Day 10 cumulative recitation of Days 6-9; Day 11 full cumulative recitation of all work so far; then repeat. The final full-surah test is always a separate final day. If 2+ new-work sessions happen after the last cumulative review before the final test, the second-to-last day becomes a partial cumulative recitation for those leftover sessions; if only one new-work session happened, the app goes straight to the final full-surah test. The dashboard returns these review-only items as `todaysPlan.newMemorization` with `isReviewOnly: true`, and mobile 2I.1 shows them as `Recitation Focus`/`Recitation` in the overview cards. Phase 2I.2a through 2I.4, Phase 2J.1-2J.3, and Phase 2K.1 are hardware-tested.
- Phase 2I.2a hardware QA notes: Mohammad tested the Pause & Save completed-to picker on iPhone and reported QA passed. Partial saves, full-range saves, review-only finish recitation, `Keep Practicing`, and the Needs Work -> Practice smoke all passed.
- Phase 2I.2b implementation notes: mobile `memorization.tsx` now protects active memorization-session exits with a leave confirmation sheet. The visible session Back button opens `Save & Leave`, `Leave without saving`, and `Keep Practicing`; `usePreventRemove` also intercepts active route-removal/back gestures where React Navigation exposes `beforeRemove`. `Save & Leave` reuses the existing Pause & Save flow, so normal sessions still choose completed-to ayah before rating and review-only sessions remain all-or-nothing. `Leave without saving` stops recite recognition/audio, unloads sound, clears the active session, returns to the Memorize overview, and intentionally avoids memorization/daily-progress POSTs. `Keep Practicing` only dismisses the sheet and preserves session/audio state where possible. No frozen web files, generated files, backend/spec files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2I.2b hardware QA notes: Mohammad tested the leave modal on iPhone and reported QA passed. Active-session Back, `Keep Practicing`, normal and review-only `Save & Leave`, `Leave without saving`, and active-session back/abort behavior passed.
- Phase 2I.2c implementation notes: mobile `memorization.tsx` now has a dedicated full-screen Recitation Check surface replacing the old bottom rating sheet. The surface is reached from Pause & Save / Save & Leave after the completed-to picker, from review-only Finish Recitation, from normal session-complete, and from recite-mode completion. It keeps the existing rating values (`Needs Work` = 2, `Good` = 4, `Excellent` = 5), keeps normal-session partial completed-to saves, keeps review-only all-or-nothing saves, and shows a Recite to NoorPath score card when entered from recite mode. The save handler and daily-progress sync are unchanged after the check rating. No frozen web files, generated files, backend/spec files, tajweed implementation, recite matcher behavior, or native dependencies were touched.
- Phase 2I.2c validation notes: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check` passed locally.
- Phase 2I.2c hardware QA notes: Mohammad reported hardware QA passed after commit `6d85d59`.
- Phase 2I.2d implementation notes: mobile `memorization.tsx` now inserts the `Ready to Recite?` choice sheet at normal session-complete instead of jumping directly to Recitation Check. `Recite to Teacher` uses the existing teacher Recitation Check/rating path with the full assigned range selected. `Recite to NoorPath` reuses the existing recite mode without matcher changes, restarts at the assigned first ayah, and then returns through the existing NoorPath score-card Recitation Check path. The active-session leave modal temporarily hides and restores the ready sheet when `Keep Practicing` is chosen, while `Save & Leave` and `Leave without saving` preserve their existing save/no-save behavior. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- Phase 2I.2d hardware QA notes: Mohammad reported QA passed.
- Phase 2I.2e implementation/QA notes: mobile `memorization.tsx` now exposes a `Just Get Tested` shortcut for review-only overview assignments. The shortcut starts the existing session data load with a direct-check flag, then opens the existing teacher Recitation Check for the full review-only assignment range as soon as the range is loaded, without starting audio playback. Review-only saves remain all-or-nothing because the existing Recitation Check save path still clamps review-only completion to the full `ayahEnd`. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; Mohammad hardware-tested it after commit `e654ceb` and confirmed it works.
- Next up skip-gap implementation notes: backend dashboard scheduling now bases `Next up` and full-surah completion on the explicit contiguous memorized ayah prefix instead of raw `versesMemorized` counts, so a saved non-contiguous range cannot make ayahs 17-49 look complete after only ayahs 1-16 were learned. Dashboard daily-progress selection now prefers the earliest matching workflow row over newer duplicate same-day rows, `POST /daily-progress` reuses an existing same-day row instead of creating another memorization row for the same date, and `POST /memorization` daily-progress auto-updates select the row overlapping the just-saved session instead of blindly updating the newest row. Local validation passed with focused API typecheck, mobile typecheck, and `git diff --check`; implementation commit is `3f354f4`.
- Today's/Current work freeze follow-up: Mohammad confirmed the `Next up` contiguous-gap behavior looked good, but then `Today's work` and `Current work` were updating to match the moving `Next up` target. The API dashboard now treats the first same-day memorization target row as the frozen daily assignment, no longer rewrites an existing `not_started` target just because the computed next target changes, and avoids falling back to the scheduled next workflow item when a frozen daily target already exists. `Next up` remains free to move from progress. Local validation passed with focused API typecheck, mobile typecheck, and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final freeze-follow-up HEAD.
- Today's/Current work freeze correction: Mohammad clarified the intended behavior with examples: `Today's work` is the full daily envelope and must stay frozen all day; `Current work` moves within that envelope, then locks on the last completed item inside it; completed `Today's work` and `Current work` cards should be disabled; only `Next up` should keep moving after completion. The API dashboard now derives workflow `Next up` from the item after the frozen daily assignment instead of repeating the frozen item if progress data lags, and the non-workflow fallback excludes a completed same-day target from `Next up`. Mobile overview cards now show `Done` and are non-pressable once `Today's work`/`Current work` are complete. Local validation passed with focused API typecheck, mobile typecheck, and `git diff --check`.
- Today's/Current work freeze correction hardware QA result: Mohammad reported QA passed after commit `c479103`. This clears the three-card freeze behavior, completed-card disabled state, moving `Next up`, and smoke coverage around 2I.2e/2I.2a/2I.2b/2I.2c/2I.2d. `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced at `c479103136da96ec4b8da9296c2b2ec23e1600b2` before this docs update. Next implementation item is Phase 2I.2f Pre-session setup.
- Active branch/SHA at Phase 2I.2f implementation start: `main` at `107edbc`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `107edbcbdfda92f1250bb055defa80c1dd254ede`. Phase 2I.2f is implemented locally in the active Expo app: overview/resume/surah starts now open a pre-session setup screen before playback; setup includes bounded From/To ayah controls, repeat count 1-10, auto-advance, cumulative review, conditional review repeat count, and surah previous/next plus picker navigation; review-only assignment ranges stay locked to preserve all-or-nothing save semantics and keep `Just Get Tested`; the existing direct `Just Get Tested` shortcut still routes straight to Recitation Check. The existing `beginSession`, Pause & Save, Save & Leave, Recitation Check, Ready to Recite, recite mode, save persistence, and daily-card freeze semantics are reused. Auto-advance now advances through the selected range in ayah and page modes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2I.2f local-validation HEAD.
- Phase 2I.2f hardware QA result: Mohammad reported QA passed after commit `dc7bf14`. This clears the setup-before-playback handoff from overview/resume/surah starts, bounded From/To controls, repeat count 1-10, auto-advance through the selected range, cumulative review and review repeat count controls, surah navigation/picker for normal sessions, review-only locked ranges, direct `Just Get Tested`, and smoke coverage for existing 2I.2a/2I.2b/2I.2c/2I.2d/2I.2e behavior plus daily-card freeze semantics. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `dc7bf1436b86924762be271467d74f854cc08db1`; after this docs commit, sync all four refs to the final Phase 2I.2f hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2g implementation start: `main` at `22c766a`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `22c766a9e3e2ba86ca9546954304812ef02d36c3`. Phase 2I.2g is implemented in the active Expo app: `src/lib/settings.ts` now has per-child AsyncStorage helpers for memorization session bookmarks; `memorization.tsx` saves the bookmark as active session range/current ayah/settings change; the Memorize discovery page shows a distinct `Resume where you left off` card; tapping it restores surah, From/To, current ayah, repeat count, auto-advance, cumulative review, review repeat count, review-only flag, and page range where available, then starts playback at the saved ayah; successful Recitation Check saves clear the bookmark; `Leave without saving` clears it intentionally; starting a newer session replaces it through the session-state save effect. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Implementation/docs commit `b152e27` was pushed and synced to `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch`.
- Phase 2I.2g hardware QA result: Mohammad reported QA passed after commit `b152e27`. This clears the `Resume where you left off` card, saved ayah/range/settings hydration, successful-save bookmark clearing, `Leave without saving` bookmark discard, and newer-session bookmark replacement. At this docs-sync start, all four refs were synced to `b152e271c107355b441bd5ffa38afb2729e32623`; after this docs commit, sync all four refs to the final Phase 2I.2g hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2h implementation start: `main` at `a5f2d09`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `a5f2d09b2f30ca4d57436ee7017d187d71cce348`. Phase 2I.2h is implemented in the active Expo app: the active memorization session now has a `View in Full Mushaf` action in the controls island; it pauses visible playback/auto-advance before navigation without saving or discarding the session, computes the active Mushaf page from loaded verse metadata with safe fallbacks, and pushes the Full Quran reader with page/surah/ayah context. The mobile Full Quran reader now accepts a `page` route param, opens on that page instead of the saved reading page when provided, and shows a small Memorization context pill while preserving normal Back navigation to the still-mounted session. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- Phase 2I.2h hardware QA result: Mohammad reported QA passed after implementation commit `36622e5`. This clears opening Full Quran/Mushaf from the active memorization session at the active page/ayah context, normal Back returning cleanly to the still-mounted memorization session, playback/auto-advance pausing before navigation, and smoke coverage for review-only assignments, `Just Get Tested`, bookmark/resume, Pause & Save, Save & Leave, Ready to Recite, Recitation Check, daily-card freeze semantics, ayah/page modes, cumulative review, audio highlighting, reciter/speed settings, blind/blur modes, translation popup, recite mode, and mark-complete persistence. At this docs-sync start, all four refs were synced to `36622e5ad0805e491a886b4f636c900c933915ef`; after this docs commit, sync all four refs to the final Phase 2I.2h hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2i implementation start: `main` at `2b8524f`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `2b8524f3f0cbbe8c5019e27d47682b6338f5edd3`. Phase 2I.2i is implemented in the active Expo app: active memorization sessions now show `Skip Repeat` and `Skip Ayah` controls under the Prev / Play / Next row. `Skip Repeat` advances the normal repeat pass, treats the last repeat as the existing repeat-cycle completion path, advances cumulative review to the next pass when possible, and exits/advances after the final cumulative pass. `Skip Ayah` stops current playback and advances the single-phase cursor to the next ayah or existing session-completion flow; during cumulative review it exits/advances from the review without changing the single-phase cursor incorrectly. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Implementation/docs commit `68b6769` was pushed to `origin/main`; `feature/main-working-branch` was fast-forwarded and pushed to the same commit before this docs-only sync.
- Phase 2I.2i hardware QA result: Mohammad reported QA passed after docs sync commit `3bcb7b0`. This clears Skip Repeat in single-phase repeat sessions and cumulative review, Skip Ayah in single-phase / final-ayah / cumulative-review cases, and smoke coverage for review-only assignments, `Just Get Tested`, bookmark/resume, Pause & Save, Save & Leave, Ready to Recite, Recitation Check, View in Full Mushaf, daily-card freeze semantics, ayah/page modes, cumulative review, audio highlighting, reciter/speed settings, blind/blur modes, translation popup, recite mode, and mark-complete persistence. At this docs-sync start, all four refs were synced to `3bcb7b01adec07efd4f201dd657c3a296bb3334a`; after this docs commit, sync all four refs to the final Phase 2I.2i hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2j implementation start: `main` at `c8cf9f4`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `c8cf9f48b9663566e0ea17677b2a85244778f6f6`. Phase 2I.2j is implemented in the active Expo app. Active ayah-mode sessions now show a tappable ayah end marker, and page-mode Quran.com end markers are tappable when the ayah is in the session range, not in recite mode, and not hidden by blind mode. Tapping opens an Ayah actions sheet with focused actions only: `Practice from this ayah` safely resets the current session cursor to that ayah, and `View in Full Mushaf` opens the existing Full Quran reader at that ayah/page after pausing playback. Existing word tap-to-seek and long-press translation handlers remain unchanged. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2I.2j local-validation HEAD.
- Phase 2I.2j hardware QA result: Mohammad reported QA passed after implementation/docs commit `5826666`. This clears the ayah-mode marker, page-mode marker, `Practice from this ayah`, `View in Full Mushaf`, recite-mode gating, blind-mode hidden/revealed gating, word tap-to-seek, and long-press translation smoke. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `5826666fedd74c910a3a2f70093cf51d1e8f2500`; after this docs commit, sync all four refs to the final Phase 2I.2j hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2k implementation start: `main` at `b8bdf41`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `b8bdf41d3eca68075ce8a133119a79037032a72d`. Phase 2I.2k is implemented in the active Expo app with implementation commit `4e70e1a`: tapping a visible active-session word opens a compact word tooltip with Arabic text, English translation/fallback, word position, and a WBW audio play button; ayah-mode taps still seek to the word, and page-mode tooltip support is scoped to the active verse while other in-range page words keep their existing seek behavior. WBW audio uses `https://audio.qurancdn.com/wbw/{surah}_{verse}_{position}.mp3` with zero-padded 3-digit values via a JS-only helper and a separate short-lived `expo-av` sound so the main recitation sound is not reused. Existing long-press translation popup, ayah sheet, review-only assignments, `Just Get Tested`, bookmark/resume, Pause & Save, Save & Leave, Ready to Recite, Recitation Check, View in Full Mushaf, Skip Repeat / Skip Ayah, daily-card freeze semantics, ayah/page modes, cumulative review, audio highlighting, reciter/speed settings, blind/blur modes, recite mode, and mark-complete persistence were preserved. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Mohammad hardware-tested the slice after final sync `b7fcdd9` and reported QA passed.
- Active branch/SHA at Phase 2I.2l implementation start: `main` at `b7fcdd9`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `b7fcdd939b586293720e5000ff7146ab306ed0e8`. Phase 2I.2l is implemented in the active Expo app with commit `15571ae`: page-mode memorization sessions now explicitly keep the active ayah in view and retry briefly after page/line layout, so recite-mode verse advancement pulls the next Mushaf page into view when crossing a page boundary. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; Mohammad hardware-tested it and reported QA passed. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `15571ae83b90f032dbab4a224875f173ed4b4d59`; after this docs commit, sync all four refs to the final Phase 2I.2l hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2m implementation start: `main` at `72004ed`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `72004ed2d053c36c13e00d26dbb60df25dc39102`. Phase 2I.2m is implemented in the active Expo app with implementation/docs commit `ad9a825`: recite mode now shows `Show Word` and `Skip Word` floating pills above the controls island; `Show Word` reveals/unblurs only the current expected word; `Skip Word` reveals/penalizes the current word, advances to the next expected word or next ayah, and keeps completion routed through the existing Recitation Check score-card path. The implementation adds web-style revealed-word and recite-attempt bookkeeping without tightening the matcher. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher strictness changes, or native dependencies were touched. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `ad9a825ae21b046644f6bdd2ced6eaf6480a0384`; after this docs commit, sync all four refs to the final Phase 2I.2m synced-docs HEAD. Hardware QA follow-up: Mohammad reported that recite hidden words and the `Blur other verses while playing` setting looked like grey outlines/blocks instead of actual blur. The mobile follow-up commit `c4d06ba` replaces the `expo-blur` overlay rendering with transparent text fill plus blurred text shadows for recite hidden words and page-mode inactive words, removes the pending-word grey fill, keeps `Show Word` unblurring only the current expected word, and makes no matcher, tajweed, backend, frozen-web, generated-file, native dependency, or persistence changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Mohammad hardware-tested after `c4d06ba` and reported QA passed, clearing real blur, `Show Word`, `Skip Word`, cursor advance, and Recitation Check completion. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `c4d06ba4fca2316105df02c8a54684760db83c47`; after this docs commit, sync all four refs to the final Phase 2I.2m hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.2n implementation start: `main` at `24c9831`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `24c98318298296c72c62f5b68a93a275c12f9812`. Phase 2I.2n is implemented in the active Expo app with implementation/docs commit `56b2905`: recite-mode Full Mushaf sessions now track the displayed page, expose previous/next page pills when the selected recite range spans multiple pages, and show a `Current word` jump pill when the displayed page differs from the active recite word's page. Existing page auto-flip, Show Word / Skip Word, real blur, Recitation Check completion, matcher behavior, review-only assignments, `Just Get Tested`, bookmark/resume, Pause & Save, Save & Leave, Ready to Recite, View in Full Mushaf, Skip Repeat / Skip Ayah, ayah sheet, tapped-word tooltip/WBW audio, daily-card freeze semantics, cumulative review, audio highlighting, reciter/speed settings, blind/blur modes, mark-complete persistence, and tajweed backlog status are preserved. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- Phase 2I.2n hardware QA result: Mohammad reported QA passed after commit `56b2905`. This clears multi-page recite navigation pills in Full Mushaf mode, previous/next page pill visibility, `Current word` jump behavior, page auto-flip across page boundaries, Show Word / Skip Word, real blur, and Recitation Check completion. At Phase 2I.2o start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `56b290537ffdaf9f546084dd93b8a3b59545f494`.
- Active branch/SHA at Phase 2I.2o implementation start: `main` at `56b2905`, with all four tracked refs synced. Phase 2I.2o is implemented locally in the active Expo app: a JS-only native celebration overlay mirrors the web `CelebrationOverlay`, and completed session/range saves plus newly completed surahs now show confetti instead of the plain success Alert. Partial Pause & Save still uses the existing simple saved Alert, and error Alerts are unchanged. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; after this implementation/docs commit, sync all four refs to the final Phase 2I.2o local-validation HEAD.
- Phase 2I.2o hardware QA result: Mohammad reported QA passed after commit `2226cac`. This clears completed-session celebration overlay, newly completed-surah celebration overlay, overlay dismissal, and partial Pause & Save still using the simple saved Alert. At Phase 2I.3 start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `2226cacdf92ce5ba7957a3fba04248666412af07`.
- Active branch/SHA at Phase 2I.3 implementation start: `main` at `a78b99d`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `a78b99d0a2f7f1fd3d81300e4304912274f4ad5d`. Phase 2I.3 is implemented and locally validated in the active Expo app: `settings.ts` now persists `cumulativeReview`, `reviewRepeatCount`, and `confetti`; `targets.tsx` exposes those controls plus visible reciter/theme/view-mode defaults in Parent Settings; Memorize discovery/setup exposes a `Defaults` action; and new session setup starts reload parent defaults while bookmark resume keeps bookmark-specific settings. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. No frozen web files, backend/spec/generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2I.3 local-validation HEAD.
- Phase 2I.3 hardware QA result: Mohammad reported QA passed after commit `bcfcab4`. This clears Parent Settings controls for cumulative review/review repeat/confetti/reciter/theme/view mode, Memorize `Defaults` discoverability, new setup hydration, bookmark resume preserving bookmark values, and confetti opt-out for the 2I.2o overlay. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `bcfcab47ac2a3620e8b16cdf3a761aa85c7ec63c`. Remote sync target after this docs commit: all four refs synced to the final Phase 2I.3 hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2I.4 implementation start: `main` at `a8b22cb`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `a8b22cbfafe89190d0d1dee45a2be71033540120`. Phase 2I.4 is implemented and locally validated in implementation commit `5685613`: the active mobile Memorize discovery rows now mirror web `getReviewStrengthTone` with no tone for zero memorized ayahs, red for any memorized ayah at strength <= 1, orange for partial rows or fully memorized rows with any strength <= 3, and green only for fully memorized strong rows. Rows get a compact tone rail/progress color/pill without changing start/resume behavior. The `/api/surahs` summary now includes `tajweedNotes`, OpenAPI and generated client/Zod outputs were regenerated, mobile `SurahSummary` exposes optional notes, and discovery rows show a contained Tajweed Notes accordion when notes exist. In-session tajweed coloring remains backlogged. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit`, `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, and `git diff --check`. Mohammad hardware-tested Phase 2I.4 after docs commit `8674e45` and reported QA passed. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `8674e45fb1d44b53aab31429d4bd3b663dff6134`. Remote sync target after this docs commit: all four refs synced to the final Phase 2I.4 hardware-confirmed docs HEAD. Next action is Phase 2J.1 review completed-today and continue-next.
- Active branch/SHA at Phase 2J.1 implementation start: `main` at `c74e4a2`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `c74e4a2dd78090db66fc9bdd0ebc4a3257149a45`. Phase 2J.1 is implemented locally in the active Expo app: Review now keeps an active review date, fetches the existing `/reviews` payload for that date, preserves API-provided `reviewedToday` rows, and shows `Continue Reviewing` from complete/empty day states when upcoming API data can identify the next open review day. The existing review-session route is preserved and now carries the active review date into the existing SM-2 submit call via `x-local-date`; `apiFetch` now respects a caller-provided local date instead of overwriting it. No frozen web files, backend files, generated files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA is pending; remote sync target after this implementation/docs commit is all four refs synced to the final Phase 2J.1 local-validation HEAD.
- Phase 2J.1 hardware QA result: Mohammad hardware-tested Review completed-today / Continue Reviewing after commit `949fa3b` and reported QA passed. This clears completed review rows staying visible after returning from a session, `Continue Reviewing` into the next open review day, existing SM-2 submission behavior with the active local date, dashboard/review refresh smoke, and existing due/upcoming/reviewed sections. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `949fa3b83b36becaaaaa1b916d435fe72427932a`. Remote sync target after this docs commit: all four refs synced to the final Phase 2J.1 hardware-confirmed docs HEAD. Next action is Phase 2J.2 ahead-day local session behavior.
- Phase 2J.2 hardware QA result: Mohammad hardware-tested ahead-day local review session behavior after final sync `06c11ec` and reported QA passed. This clears active review date persistence after navigation/refresh, restored completed active-day rows, next-open review day continuation, grouped completed prior-day sections, existing SM-2 submission via `reviewDate`/`x-local-date`, dashboard/review refresh smoke, bottom nav review count smoke, and existing due/upcoming/reviewed sections. Research note for the next slice: the web app's "review multiple" feature is `Review A Few Together` in `artifacts/noor-path/src/pages/review.tsx`; it is the connected Mushaf batch review flow, and TODO now tracks it explicitly as Phase 2J.3. At this docs-sync start, all four refs were synced to `06c11ecce5ed9969ecee49cb4dd3604f60f9324c`; after this docs commit, sync all four refs to the final Phase 2J.2 hardware-confirmed docs HEAD.
- Active branch/SHA at Phase 2J.3 implementation start: `main` at `a553a66`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `a553a66795393692c48ef178725f1128be80aa2a`. Phase 2J.3 Review Multiple is implemented locally in the active Expo app: Review now shows `Review A Few Together` when more than one pending review remains, supports `Select All`/`Clear All`, lets the child build one connected Mushaf run by using the corner selection circles and expanding only through neighboring pending cards, and starts the existing review-session route with a connected batch queue. The review session now tracks queue position plus next-surah context, rates each selected item through the existing SM-2 `submitReview` helper with the active `reviewDate`, advances after each rating, and supports `Skip This Surah` without submitting so the skipped surah stays pending while the queue moves forward. Web flashcard mode and richer review celebration overlay were triaged as post-Review-Multiple extras, likely post-beta unless Mohammad reorders, because the beta-critical missing review behavior was the connected Mushaf batch path and no API gap is needed for those extras. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; no backend/spec/generated files, frozen web files, tajweed implementation, recite matcher behavior, or native dependencies were touched. Hardware QA later passed after `ae74d1d`, as recorded below.
- Phase 2J.3 hardware QA result: Mohammad hardware-tested Review Multiple after commit `ae74d1d` and reported QA passed. This clears `Review A Few Together` appearing with multiple pending reviews, connected neighboring-card selection, `Select All`/`Clear All`, `Start Mushaf` opening one batch queue, queue position/next-surah context, rating through the queue, `Skip This Surah` keeping skipped items pending while advancing, completed rows/session persistence, dashboard/review refresh, and bottom-nav count smoke. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `ae74d1d2fe7b29ef5279aeedbf08f0d6599381db`. Remote sync target after this docs commit: all four refs synced to the final Phase 2J.3 hardware-confirmed docs HEAD. Next action is Phase 2K.1 Mushaf tool shell.
- Active branch/SHA at Phase 2K.1 implementation start: `main` at `2fe6726`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced to `2fe6726e887557246212b7f5bc1ea643414e7a34`. Phase 2K.1 is implemented locally in the active Expo app: `artifacts/noor-mobile/app/child/[childId]/mushaf.tsx` keeps the existing PNG page reader and reading-progress save path, adds collapsible chrome/tool controls, page/surah/juz/bookmark/recent-read jump affordances, persisted recent reads, blind mode, select mode, recite-pick entry points into the memorization route, and a Quran.com-backed page ayah list plus ayah action sheet. No frozen web files, generated files, backend/API code, tajweed code, recite matcher logic, native dependencies, or page-image replacement were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2K.1 local-validation HEAD. Next action is hardware-test 2K.1 before starting 2K.2.
- Phase 2K.1 hardware QA result: Mohammad hardware-tested the Mushaf tool shell after commit `342204b` and reported QA passed. This clears Full Quran dashboard entry, RTL page swiping, Prev/Next/presets, save/autosave resume behavior, Jump page/surah/juz/bookmark/recent reads, chrome collapse/expand, Blind cover/reveal/page-change reset, Select -> selected Memorization range handoff, Recite -> chosen-ayah Memorization handoff, normal ayah action sheet, dashboard/review refresh smoke, and bottom-nav review count smoke. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `342204be20de5e447585b5a9ae0208bcc46f2f04`. Remote sync target after this docs commit: all four refs synced to the final Phase 2K.1 hardware-confirmed docs HEAD. Next action is Phase 2K.2 ayah sheet and annotations.
- Active branch/SHA at Phase 2K.2 implementation start: `main` at `ba87078`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `ba870788c9a98c148d10f6b5564fde950ba4af95`. Phase 2K.2 is implemented locally in the active Expo app: the Full Quran ayah sheet now has mobile-native subviews for bookmark/highlight/note, selected ayah metadata, previous/next ayah navigation within the page, per-child AsyncStorage annotation persistence, annotation chips in the page ayah list, ayah bookmarks in Jump, and optional Quran.com translation/tafseer/WBW plus native Share. Existing page-image reader, page paging/autosave, Jump/Blind/Select/Recite shell, recite-pick/memorization handoffs, memorization/review behavior, frozen web files, backend/spec/generated files, native dependencies, tajweed, and recite matcher behavior were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2K.2 local-validation HEAD. Mohammad should hardware-test 2K.2 before any 2K.3 work starts.
- Phase 2K.2 hardware QA result: Mohammad hardware-tested the ayah sheet and annotations after commit `a931258` and reported QA passed. This clears ayah metadata and previous/next navigation, bookmark/highlight/note persistence, ayah list annotation chips, Jump -> Ayah bookmarks, Translation/Tafseer/WBW panels, native Share, existing `Practice from here`, sheet `Add to selection`, Select `Open Range`, Recite chosen-ayah handoff, page-image swiping, save/autosave resume, Jump page/surah/juz/page bookmarks/recent reads, Blind, chrome collapse, dashboard/review refresh, and bottom-nav review count smoke. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `a931258c69f2a0704eaeb3ffc36b8760c3799d4a`. Remote sync target after this docs commit: all four refs synced to the final Phase 2K.2 hardware-confirmed docs HEAD. Next action is Phase 2K.3 audio range / recite / select-to-memorize.
- Active branch/SHA at Phase 2K.3 implementation start: `main` at `76169e8`, with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced to `76169e8a49bb8a1abdacbbdc46cb577e7491d38c`. Phase 2K.3 is implemented locally in the active Expo app: Full Quran now has page/ayah/selected-range audio playback using existing EveryAyah audio helpers, a mini player with play/pause/skip/stop, persisted per-child reciter/speed/per-ayah-repeat/range-repeat settings, ayah-sheet `Play ayah` / `Play from here` actions, selected-ayah `Play` actions, recite-from-ayah/selected-range handoff into the existing memorization recite matcher via route params, and selected/single ayah `Mark memorized` actions using the existing memorization update path plus conservative same-day daily-progress sync for contiguous current-day targets. Existing page-image reader, page paging/autosave, Jump/Blind/Select/Recite shell, 2K.2 annotations, Phase 2I memorization behavior, Phase 2J review behavior, bottom-nav review count behavior, frozen web files, backend/spec/generated files, native dependencies, tajweed, and recite matcher strictness were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2K.3 local-validation HEAD. Mohammad should hardware-test 2K.3 before any Phase 2L/TestFlight readiness work starts.
- Phase 2K.3 hardware QA result: Mohammad hardware-tested audio range / recite / select-to-memorize after commit `48d52a9` and reported QA passed. This clears Full Quran page/ayah/selected audio playback, audio settings, repeat behavior, mini-player controls, recite handoff through the existing matcher, mark-memorized updates, dashboard/memorization refresh smoke, and preservation of existing page-image reader, paging/autosave, Jump/Blind/Select/Recite shell, 2K.2 annotations, review behavior, bottom-nav review count, tajweed backlog status, and matcher behavior. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `48d52a9a765e0ea25cb5375b7dd0a9fff152d6b7`. Remote sync target after this docs commit: all four refs synced to the final Phase 2K.3 hardware-confirmed docs HEAD. Next action is Phase 2L.1 visual polish pass; do not jump straight to TestFlight build until 2L.1/2L.2 readiness items are handled and tested.
- Active branch/SHA at Phase 2L.1 implementation start: `main` at `61d332e` (`61d332eed493d316236b4955f3eec1a597a7afee`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Phase 2L.1 visual polish is implemented locally in the active Expo app only: shared screen primitives, bottom nav, sign-in/onboarding/profile management, dashboard, More/settings/targets, Memorize discovery/setup/session/Recitation Check, Review/review-session, and Full Quran/Mushaf now share a cleaner `#f8fafc` shell, white scan-friendly cards, tighter headers, consistent borders, stronger status/empty/error states, and elevated bottom control/nav surfaces. No frozen web files, backend/spec/generated files, native dependencies, tajweed rendering, or recite matcher behavior were touched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this implementation/docs commit: all four refs synced to the final Phase 2L.1 local-validation HEAD. Hardware visual screenshots/smoke later passed after `55b7cc7`, as recorded below.
- Phase 2L.1 hardware QA result: Mohammad hardware-tested the visual polish screenshots/smoke after commit `55b7cc7` (`55b7cc721caaa17f975415aa538d0c23f1c0a491`) and reported QA passed. This clears onboarding/profile management, child dashboard, More/settings/targets, Memorize discovery/setup/session/Recitation Check, Review/review-session including Review Multiple entry/session, Reading/Full Quran/Mushaf tools/audio/ayah sheet, and bottom nav shell visual smoke. At this docs-sync start, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` were synced to `55b7cc721caaa17f975415aa538d0c23f1c0a491`. Remote sync target after this docs commit: all four refs synced to the final Phase 2L.1 hardware-confirmed docs HEAD. Next action is now Phase 2L.0 UI refinement + backend QA pass before 2L.2 app shell/assets and 2L.3 TestFlight build/QA.
- Active branch/SHA at Phase 2L.0 docs setup start: `main` at `3a77bde` (`3a77bde13101771e174460340deb18187069b26e`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Phase 2L.0 UI refinement + backend QA pass is now active between 2L.1 and 2L.2. Mohammad will provide the UI/backend change list; each item becomes its own focused Codex slice with validation, branch sync, and iPhone hardware QA before the next slice. Do not start Phase 2L.2 app shell/assets until Mohammad explicitly says 2L.0 is done. Remote sync target after this docs commit: all four refs synced to the final Phase 2L.0 docs-introduction HEAD.
- Active branch/SHA at Slice 2L.0.b implementation start: `main` at `28f1640` (`28f16409c21f36706c9afda4d388db23332a1ee6`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Slice 2L.0.b is implemented locally in implementation commit `13852e0` (`13852e0c3239d29ff1720713b9c2205a4a1a2099`): the active mobile dashboard now has the compact back/avatar-name/gear top bar, child switcher sheet with Add Child, read-only infinite date scrubber, no black Today banner, and disabled/read-only work-card behavior for non-today dates; the API dashboard route now honors `?preview=true` by synthesizing missing daily-progress state without inserting/backfilling rows. No frozen web files, native dependencies, tajweed implementation, or matcher behavior were touched. Local validation passed with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.b local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.c implementation start: `main` at `f0731aa` (`f0731aa13a8c7ca33fa781d5ec6745e0857c8a91`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Slice 2L.0.c is implemented locally in implementation commit `277bb5e` (`277bb5effdef51000d007f902fe1569b416bc702`): the active mobile dashboard body now uses the actual date in the date scrubber with a non-today `Today` reset pill, a slim inline stats banner, a primary-size `Today's Work` heading, Story/Du'a compact cards directly below the task cards, the preserved Up Next card, a combined Goals/Achievements tab card, and reduced Quick Actions for Plan, Progress, and More. JS-only; no backend, native dependency, frozen web, tajweed, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.c local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.e implementation start: `main` at `7529f55` (`7529f559d88206826404b1ab9ac53fe01d31b103`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Slice 2L.0.e is implemented locally in implementation commit `340ab81` (`340ab81c5d85b8a69f6f1c972164c3760e487698`): the active mobile app now has `/child/[childId]/plan` with Progress-style top chrome, age-group header, practice-minutes badge, goals with reset-to-auto confirmation/refresh, description, current phase, weekly goals, milestones, and tajweed-rules text; the dashboard Plan Quick Action and Goals tab link now route to the Plan page. JS-only; no backend, native dependency, frozen web, tajweed implementation, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. This completes the Plan/Progress dashboard wiring originally scoped across slices 2L.0.c through 2L.0.e. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.e local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.p implementation start: `main` at `04db499` (`04db499eaeaa3c43dc58359f05e81ca3af03b1ab`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` all synced. Slice 2L.0.p is implemented locally in implementation commit `80663ff` (`80663ff86e531e7ebd84f1bd83b537b0338646e4`): the active mobile Review Session now has the consolidated two-line header with page range, no redundant review-range banner, fixed ayah strip with active highlight, tap-to-seek, long-press translation popup using shared Quran.com translation ID 20, and swipe/scroll Mushaf page layout. The new profile-level `mushafViewMode` default is separate from the existing memorization Ayah/Page `viewMode`; it defaults to `swipe` for a more book-like Mushaf experience while existing users keep their Ayah/Page default untouched. The same Mushaf view preference now applies to Review Session and memorization Full Mushaf, with per-session override and Save as default in both sheets. JS-only; no backend, native dependency, frozen web, tajweed, matcher, or audio-engine changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.p local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.s implementation start: `main` at `8e0b6ed` (`8e0b6eda4503d10edabfb202dd4b100008cd4fe3`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.s is implemented locally in implementation commit `6c126a5` (`6c126a5becd245f0dbb1df12ed690f7a0e86cd15`): `review_schedule` now has nullable `last_reviewed_local_date`, `POST /children/:childId/reviews` persists the request-local `YYYY-MM-DD` on update and insert, and `GET /children/:childId/reviews` filters `reviewedToday`/Due Today exclusion by that explicit user-local string instead of server-local `Date` accessors. Mobile Review now hides Upcoming Reviews while `dueToday` is non-empty and keeps its section spacing based only on Reviewed Today when today is clear. Schema push is required on Mohammad's machine before production sees the new column: `DATABASE_URL='<prod URL>' pnpm --filter @workspace/db push`; expected drizzle-kit prompt is adding `last_reviewed_local_date` to `review_schedule`; abort if any prompt suggests dropping or renaming any other column. Local validation passed with `npx tsc --build lib/db` to refresh local DB declarations, `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, `npx tsc --build lib/db`, and `git diff --check`. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.s local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test after schema push.
- Active branch/SHA at Slice 2L.0.t implementation start: `main` at `f58ae54` (`f58ae54cc4fb20189342f03bec1de1e53c09081f`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.t is implemented locally in implementation commit `9acea01` (`9acea016d3513452d31a719c43280b68be56b371`): `GET /children/:childId/reviews` now reads/writes `review_daily_set` only for the request-local real today and computes every future `continueOffset` day fresh from current `review_schedule.due_date <= queryDate` candidates with the existing budget and wrap fallback. This fixes the 2L.0.r/2L.0.s bug where future-day snapshots could be frozen before a review rating changed a surah's due date, causing Continue Reviewing to miss fresh tomorrow work like an Al-Fatiha red rating. The actual today frozen set still excludes surahs already reviewed today, while promoted future days may include a reviewed-today surah again when its new due date/fallback makes it tomorrow work. Mobile Review now has only the text-baked `Continue Reviewing →` arrow and no separate icon, and the Upcoming Reviews preview list is removed while the QueueSummary count remains. No schema migration or drizzle-kit push is required; stale future-date `review_daily_set` rows are ignored. Local validation passed with `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, `npx tsc --build lib/db`, and `git diff --check`. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.t local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.u implementation start: `main` at `4486a54` (`4486a54dd76ce88d3790f2597364ed085170f316`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.u is implemented in commit `624037e` (`624037e8bf79b080ea665325f136b824c46e473c`): mobile Review no longer deduplicates `dueToday` or `upcoming` against `reviewedToday`, so it trusts the backend to allow intentional re-review cards during a wrap lap; `GET /children/:childId/reviews` now detects when a future-day candidate set is entirely made of surahs reviewed today and replaces it with a fresh budgeted canonical wrap-order lap, including intermediate Continue Reviewing days. This fixes the bug where tapping Continue Reviewing after the child completed every memorized surah today could show "Today's work complete!" with no new items, even though review should never run out. The existing `dueTodayItems` filter remains unchanged, so actual today still excludes reviewed-today surahs while future continue offsets may show the same surah in Due Today and Reviewed Today at the same time. No schema migration, generated files, frozen web files, recite matcher, or tajweed changes. Local validation passed with `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, `npx tsc --build lib/db`, and `git diff --check`. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.u local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.v implementation start: `main` at `9be093f` (`9be093f9a6ac1b7d60288ccd7cee016038e8e3c3`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.v is implemented in commit `7847d37` (`7847d3776a54c5ca0a6df07ec452224afda726bd`): the active mobile Recite mode speech result handler now reads the transcript and `isFinal` before debouncing, bypasses the debounce for final speech results, and lowers the interim-only debounce from 300ms to 150ms. This fixes the hardware QA last-word stall where iOS sends the final transcript shortly after the last interim match and the cursor could stay on the final word. Existing `wordMatches`, per-word dedup refs, the final-result reset block, recite matcher strictness, backend/schema/API/generated files, frozen web files, and tajweed were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; the requested recite-mode smoke scenarios were mentally walked with no gaps found. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.v local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.w implementation start: `main` at `0696746` (`0696746355dcb0b875e2e05e2069d72a3a2154e9`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.w is implemented in commit `2878319` (`287831956cffb10eda50f8d726f7b84aabadcd8b`): added `scripts/generate-mushaf-tables.mjs` to derive `PAGE_TO_FIRST_VERSE_GENERATED` and `SURAH_START_PAGES_GENERATED` from the bundled QUL KFGQPC v4 layout/word assets, generated matching API/mobile table files, wired `quran-meta.ts` and mobile `mushaf.ts` to import those generated tables, and added read-only `scripts/verify-db-page-numbers.mjs` plus root scripts `generate:mushaf-tables` and `verify:db-pages`. This removes the stale hand-typed page table that caused Al-'Adiyat to split incorrectly and prevents future manual page-map drift. First generator diff against the old hand data: API `PAGE_TO_FIRST_VERSE` changed 25 of 604 entries; affected surahs were 5, 6, 55, 68, 69, 70, 74, 79, 80, 81, 83, 84, 85, 87, 88, 89, 90, 91, 92, 94, 95, 96, 97, 98, and 100. Largest shifts were page 598 old 97:1 -> QUL 96:13, page 597 old 95:1 -> QUL 94:3, page 592 old 87:16 -> QUL 87:11, page 596 old 92:15 -> QUL 92:10, page 593 old 89:1 -> QUL 88:23, and page 600 old 100:10 -> QUL 100:6. API/mobile `SURAH_START_PAGES` had 0 of 114 changes. Static smoke from generated tables: Al-'Adiyat 1-5 maps to page 599 and 6-11 maps to page 600 per the bundled QUL layout, so the one-page strict fractional chunk still resolves Al-'Adiyat 1-11 as a single `surah_end` target even though the prompt's page-600-starts-at-100:1 wording does not match this QUL asset. Local validation passed with `node scripts/generate-mushaf-tables.mjs`, `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, `npx tsc --build lib/db`, and `git diff --check`. Pending Mohammad action: run `DATABASE_URL='<prod URL>' node scripts/verify-db-page-numbers.mjs`; if it reports mismatches, run a follow-up slice to update `quran_verses.page_number` in Neon. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.w local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.x implementation start: `main` at `960386a` (`960386a860867df8e5657f4ae8b8c26229137ce1`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.x adds the one-shot `scripts/sync-db-page-numbers.mjs` maintenance script plus root `sync:db-pages` npm script to resync Neon `quran_verses.page_number` from the bundled QUL KFGQPC v4 layout. Root cause from Mohammad's verifier run: 56 DB rows were stale from the old hand-typed seed data, and runtime `getPageForVerse` correctly preferred the DB-backed `versePageCache`, so the 2L.0.w static table fix was being overridden after cold start. Mohammad should run `DATABASE_URL='<prod URL>' node scripts/sync-db-page-numbers.mjs` once from repo root, then restart/redeploy the Railway API service so the in-memory `versePageCache` reloads from corrected DB rows, then run `DATABASE_URL='<prod URL>' node scripts/verify-db-page-numbers.mjs` and expect all verses to match QUL. No `quran-meta.ts`, `memorization-workflow.ts`, frontend, frozen web, generated, schema, or runtime cache-loading code changed in this slice. Local validation passed with `node --check scripts/sync-db-page-numbers.mjs` and `git diff --check`. Remote sync target after this commit: all four refs synced to the final Slice 2L.0.x maintenance-script/docs HEAD. Hardware QA: pending Mohammad DB sync, Railway API restart, verifier rerun, and Al-'Adiyat iPhone test.
- Active branch/SHA at Slice 2L.0.y implementation start: `main` at `995bd08` (`995bd0867646da2a6da7c2a5d4433a62c8efdfa0`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.y fixes dashboard `upNextMemorization` for same-surah continuations when today's frozen target is only part of a surah, such as Al-'Adiyat 1-5. The API now checks the current single-surah daily target before falling back to the next canonical surah; if the target has remaining ayahs, Next Up starts at `max(todayTargetEnd + 1, contiguousMemorizedEnd + 1)` and resolves the next strict same-surah chunk. This makes Al-'Adiyat preview 6-11 both before and after ayahs 1-5 are saved, instead of showing 1-11 before save or Az-Zalzalah after completion. Backend-only; no mobile UI, frozen web, generated files, schema, `quran-meta.ts`, runtime cache loading, or sync scripts changed. Local validation passed with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, focused Al-'Adiyat scheduler smoke (`before-save { start: 6, end: 11 }`, `after-save { start: 6, end: 11 }`), and `git diff --check`. Remote sync target after this commit: all four refs synced to the final Slice 2L.0.y same-surah-next-up fix HEAD. Hardware QA: pending Mohammad iPhone retest for new child through Al-'Adiyat.
- Active branch/SHA at Slice 2L.0.z implementation start: `main` at `dba69bf` (`dba69bf9295afb489261cc1eae599a23345d96ec`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.z fixes the missing Al-'Adiyat 6-9 words in memorization Full Mushaf page mode. Root cause: the renderer used QUL KFGQPC v4 line layout for page 600, but filled it with Quran.com `verses/by_page/600`; that endpoint returns `100:10 100:11 101:1...` and omits `100:6-100:9`, while QUL page 600 lines require `100:6-100:11`. The mobile session now derives the verse keys required by the QUL page layout and fetches those verses by chapter/key, preserving QUL as the page-boundary authority while keeping the same renderer and interactions. JS-only; no backend, frozen web, generated files, schema, planner, recite matcher, tajweed, audio, review session, or Full Quran reader route changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit`, `git diff --check`, QUL asset inspection showing page 600 lines include `100:6-100:11`, Quran.com endpoint check showing `by_page/600` omits `100:6-100:9`, and chapter endpoint check showing `100:6-100:11` all report page 600. Remote sync target after this commit: all four refs synced to the final Slice 2L.0.z QUL page-word fetch fix HEAD. Hardware QA: pending Mohammad iPhone retest for Al-'Adiyat 6-11 Full Mushaf session.
- Active branch/SHA at Slice 2L.0.aa implementation start: `main` at `ed078fb` (`ed078fb21edeb58e7664a4d118e1045912c889be`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.aa fixes memorization session rapid Next ayah taps desyncing the verse tracker from audio. Root cause: `handleNext` could enqueue multiple `currentVerse` state changes while `Audio.Sound.createAsync` for the first next ayah was still loading; `playVerse` then refused later requested ayahs because `isLoadingRef` was true, so the marker could reach verse 10 while audio stayed on verse 7. The mobile session now advances `currentVerseRef` synchronously and clamped during navigation, tags every audio load with a latest-request token, creates sounds paused until the token/current verse still match, unloads stale late audio, and only clears sound refs for the same sound instance. JS-only; no backend, frozen web, generated files, schema, planner, recite matcher, tajweed, review session, Full Quran reader route, or QUL page fetching changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this commit: all four refs synced to the final Slice 2L.0.aa rapid-next audio sync fix HEAD. Hardware QA: pending Mohammad iPhone retest by hammering Next in Al-'Adiyat 6-11 while audio is playing; marker and spoken ayah should stay on the final requested ayah.
- Active branch/SHA at Slice 2L.0.ab implementation start: `main` at `0fbacc6` (`0fbacc6db1b678755cf3ee766eda1af500e39870`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.ab adds Review Session ayah-by-ayah tracking inside the static Mushaf page image. Root cause: Review Session already had `currentAyah` audio state plus the top ayah strip, but the Mushaf body was a plain PNG with no verse-level hit regions or highlight surface, so the page itself never showed the active ayah and tapping the page could not seek. The mobile Review Session now derives per-page ayah overlay segments from the bundled QUL KFGQPC v4 line layout/word map, draws a translucent blue highlight over the active ayah segment(s), and makes each overlay tappable to call the existing ayah seek/play path in both Swipe and Scroll Mushaf modes. Review-only JS change; no memorization, backend, frozen web, generated files, schema, planner, recite matcher, tajweed, Full Quran reader route, review queue, rating submission, or audio algorithm changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this commit: all four refs synced to the final Slice 2L.0.ab Review Session ayah overlay fix HEAD. Hardware QA: pending Mohammad iPhone retest in Review Session; active verse should highlight on the page and tapping a verse on the page should jump/play there.
- Active branch/SHA at Slice 2L.0.dd implementation start: `main` at `080aca1` (`080aca15b07317f7d22f4505d88ce4980ba8b315`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.dd is implemented in commit `4871684` (`48716840cb606d782b89120d1d545267f5c562f2`): mobile Review Session and Full Quran/Mushaf now share a Bayaan-styled text page renderer using bundled Bayaan fonts and Quran.com page-word data. Review Session replaces the PNG/QUL overlay visual with the Bayaan page, keeps existing review audio/rating logic, and wires ayah end markers to the existing `handleSeekAyah` path so the subtle ayah-level highlight follows `currentAyah`. Full Quran/Mushaf replaces the PNG page with the same renderer, keeps existing annotations/audio/select/recite/blind-mode paths, opens the existing `AyahActionSheet` from ayah end markers and word long-press, and keeps reciter changes scoped to audio reloads rather than page reloads. Unchanged: `memorization.tsx`, planner, review queue, backend/schema/API/generated files, frozen web, audio engine/queueing, annotation persistence, translations/tafseer/WBW fetchers, and word-level audio highlighting remains out of scope. Local validation passed with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/noor-mobile exec tsc -p tsconfig.json --noEmit`, `git diff --check`, and the requested smoke trace source-walk. Root typecheck skipped by request. Remote sync target after docs commit: all four refs synced to the final Slice 2L.0.dd local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone test.
- Active branch/SHA at Slice 2L.0.hh implementation sync: `main` at `41be1b8` (`41be1b8d03384f17424c8b834c89674ca31a3159`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this docs update. Slice 2L.0.hh ships the King Fahd Madani Hafs PNG-based Reading/Full Quran Mushaf with simplified UI: 604 bundled page PNGs (~40 MiB) from `open-mushaf-native` (MIT), `aya.json` + `specs.json` for per-ayah tap overlays, `king-fahd-page-images.ts` static require map, `king-fahd-ayah-coords.ts` coordinate scaling, and explicit Expo asset bundling via `app.json`. `mushaf.tsx` now renders `expo-image` page PNGs, preloads current +/- 1 and current + 2, adds an attribution modal crediting the King Fahd Complex and `open-mushaf-native` MIT source, removes page-jump presets/footer Prev-Jump-Next/status panel/select-to-memorize tool mode, keeps only floating Blind + Recite tools, makes the Surah pill open jump search, and consolidates reading actions into the press-and-hold ayah action sheet. Unchanged: `review-session.tsx`, memorization flows, backend/API/schema/generated files, review scheduling/session behavior, frozen webapp, recite matcher, and tajweed. Validation: staged inventory confirmed 610 paths total (604 PNGs plus `aya.json`, `specs.json`, the two King Fahd helper modules, `mushaf.tsx`, and `app.json`), implementation commit pushed to both tracked branches, docs cleanup passed `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/noor-mobile exec tsc -p tsconfig.json --noEmit` and `git diff --check`, and Mohammad hardware-confirmed the King Fahd Hafs reading mushaf with the simplified UI.
- Active branch/SHA at Slice 2L.0.jj implementation sync: `main` at `6eb77e0` (`6eb77e016f6eb407db906632c599caf3d017941a`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this docs update. Slice 2L.0.jj ships the Quran.com QuranEngine `hafs_1405/images_1920` Madani Reading Mushaf: 604 bundled 1920x3106 PNG pages (~113 MiB PNG total), `ayahinfo_1920.db`, pre-extracted `ayahinfo_1920.json` per-ayah pixel coordinates, `quran-com-1405-page-images.ts` static require map, `quran-com-1405-ayah-coords.ts` coordinate adapter, and `scripts/extract-quran-com-1405-assets.mjs`. `mushaf.tsx` now renders the QuranEngine image source for all 604 pages, scales tap overlays in native 1920x3106 image space, preserves existing page swipe, tap/long-press action sheet, blind mode, recite handoff, audio playback, jump search, attribution modal, bookmarks, highlights, and notes, and keeps the King Fahd fallback files for slice 2L.0.jj.c. Attribution now credits King Fahd Glorious Qur'an Printing Complex (Madinah print, 1405H/2003 edition; free-use grant) plus Quran.com QuranEngine (Apache 2.0). Bundle note: current added PNG total is ~113 MiB; expected net delta is about +73 MiB after the old ~40 MiB King Fahd PNG fallback is retired in 2L.0.jj.c. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; Mohammad hardware-tested page 604 and the full feature checklist and reported sharp Arabic, clean surah header banners, detailed ayah markers, and all existing reader features working.
- Active branch/SHA at Slice 2L.0.ll implementation sync: `main` at `e8d8e40` (`e8d8e403f8c9a23b62df84bd7f0afcdff8d199e8`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this docs update. Slice 2L.0.ll ships Reading Mushaf UX polish: the bottom Blind/Recite/audio control slot now has a parchment-tone background and thin top border for contrast; `Play from here` builds a queue from the tapped ayah to the end of the current surah and auto-advances pages through the existing audio page-follow behavior; Translation/Tafseer/WBW/Highlight/Note in-sheet Previous/Next is surah-bound instead of page-bound, updates the underlying Mushaf page when crossing a page, and preserves the active sheet sub-view while navigation happens; fresh sheet opens still reset to the main action view. Added `getMushafPageForVerse` in `src/lib/mushaf.ts` so cross-page targets can resolve page numbers without relying on the current page cache. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Mohammad hardware-tested and approved the slice. Backlog reminders are unchanged from the previous docs. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.ll hardware-confirmed docs HEAD.
- Active branch/SHA after Slice 2L.0.mm implementation sync: `main` at `b7fc5c9` (`b7fc5c98753a5fb4ecfba7b8a5be066942f060cb`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this docs update. Slice 2L.0.mm ships collapsible Translation/Tafseer text in the Reading Mushaf ayah action sheet: Translation clamps to 8 lines with a More/Less toggle when text is over 300 characters; Tafseer clamps to 10 lines with a More/Less toggle when text is over 400 characters; expansion state resets when navigating Previous/Next between ayahs; and typical-length content leaves the Previous/Next navigation visible without scrolling. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Mohammad hardware-tested and approved the slice. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.mm hardware-confirmed docs HEAD.
- Active branch/SHA at Slice 2L.0.nn.a implementation start: `main` at `b7fc5c9` (`b7fc5c98753a5fb4ecfba7b8a5be066942f060cb`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.nn.a is implemented in commit `03ec561` (`03ec56106275d905c948b2bdb022e842eb80e1ef`): Memorization now has a third opt-in `test-mushaf` view mode labeled `Test Mushaf`; `MushafTestPageView` renders the bundled Quran.com QuranEngine 1405H/1920px page image with horizontal RTL paging; normalized per-word glyph Pressable overlays are scaffolded with no visible behavior; and the view follows the current memorization ayah page while allowing local page swipes. Existing `ayah` word-grid and text-rendered `page` Full Mushaf paths, reading `mushaf.tsx`, backend/API/schema/shared business logic, audio sync, recite, translation, and memorization-state visualization were intentionally left untouched. Glyph normalization now fixes RTL `minX > maxX` flips and expands zero-width entries to a 24px native-space hit width, with a dev assertion covering 6,236 ayahs across 604 pages. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit`, `git diff --check`, and staged whitespace check including the new component. New component size: 373 lines; `memorization.tsx` delta: +41/-4. Backlog: subsequent phases should port one existing Full Mushaf feature per slice, starting with 1b tap-to-seek audio, 1c audio word highlighting, 1d recite mode, 1e translation popovers, and later memorization-state visualization/polish only after the foundation is hardware-approved. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.nn.a local-validation/docs HEAD. Hardware QA: pending Mohammad iPhone smoke after Metro reload.
- Active branch/SHA at Slice 2L.0.nn.b implementation start: `main` at `07b9a12` (`07b9a1290e04982ab6b31ec7afcd1adc733ef66f`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.nn.b is implemented in `artifacts/noor-mobile/src/components/mushaf-test-page-view.tsx`: tapping a word in Test Mushaf flashes the same scaled native word bounding box with a translucent amber `Animated` highlight, shows a compact debug pill like `2:30 word 3`, cancels/restarts the flash on repeated or different word taps, and clears any active flash when the pager settles on a different page. Existing `ayah` and `page` memorization renderers, reading `mushaf.tsx`, backend/API/schema/shared business logic, audio, recite, translation, callbacks to memorization business logic, and memorization-state visualization were intentionally left untouched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Component size: 517 lines (delta +144 from 373). Hardware QA: pending Mohammad iPhone smoke after Metro reload.
- Active branch/SHA at Slice 2L.0.nn.c implementation start: `main` at `a22af07` (`a22af070c3e85fd80f2f0ba7f65748a3a3f2dd6a`), with `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` synced before this work. Slice 2L.0.nn.c is implemented in commit `ac56040`: `MushafTestPageView` now accepts `currentAudioWord` (AudioWordPointer with 1-based QPC2 position) and `isAudioActive` props; each `MushafTestPage` renders a translucent green `rgba(34,197,94,0.35)` `Animated.View` overlay on the matching word glyph rect, driven by a ~150ms cross-fade (75ms fade-out + 75ms fade-in with native driver) when `currentAudioWord` changes, and a 250ms fade-out when it becomes null; auto-paging fires `onPageChange` when audio crosses a page boundary, deduped via `lastAutoPagedRef`; `memorization.tsx` derives `currentAudioWord` from the existing RAF-updated `highlightedPage` state (verseKey split + 1-based position) and passes it with `isAudioActive={isPlaying}` to `MushafTestPageView`; existing audio controls (`controlsIsland`) were already rendered outside the viewMode branch and remain accessible in test-mushaf mode without changes. Quran.com 1-based segment `wordIdx` aligns directly with QPC2 1-based glyph `position` — spot-checked on Al-Fatihah; extra ornamental positions (verse end markers etc.) in QPC2 never receive an audio segment and are silently unmatched. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Remote sync target after this docs commit: all four refs synced to the final Slice 2L.0.nn.c docs HEAD. Hardware QA: pending Mohammad iPhone smoke.

### Web-app parity audit summary

Mobile already has:
- Better Auth sign-in, persisted session, existing-child profile picker, sign-out.
- Mobile child creation/profile management through Phase 2H.1: create child, seed prior memorized surahs with per-surah strength and known ayah counts, add prior surahs by From/To range, edit profile settings/targets/practice minutes/visibility toggles, delete the selected child with confirmation, dismiss keyboard cleanly, and save without scrolling past all surahs. Hardware QA passed.
- Child dashboard with today's memorization/review/reading cards, review previews, streak/points, pull-to-refresh, target entry point, fallback behavior that keeps the dashboard usable if today's plan flakes, and first-pass bottom nav.
- Phase 2H.2 dashboard parity content is complete and hardware-tested: richer stats, goals, achievements, up-next memorization, story/du'a suggestions respecting visibility flags, better empty/completed states, and quick actions.
- More screen exposing current Full Quran/Parent Settings/Profile routes plus planned Progress/Plan/Stories/Du'aas entries.
- Shared mobile screen primitives now exist and are adopted by More and Review.
- Parent Settings screen for `memorizePagePerDay`, `reviewPagesPerDay`, `readPagesPerDay`, `practiceMinutesPerDay`, `hideStories`, `hideDuas`, profile entry, daily target bundles, and JS-only default memorization session settings.
- API/spec foundation for upcoming content and progress pages: child target/create/delete/goals contracts, dashboard progress/reading fields, review `reviewedToday`/`todayRange`, daily/reading/weekly progress, and generated client outputs are aligned through Phase 2G.4.
- Memorization core with today's assignment fetch, ayah/page modes, word-level audio highlight, repeat controls, auto-advance delay, cumulative review, reciter selection, 8 themes, playback speed, blind mode, blur mode, translation popup, recite mode, and mark-complete persistence.
- Phase 2H.6 memorization discovery is complete and hardware-tested: the Memorize route now shows current/today work, resume cards, surah search/filter, per-surah progress/strength, and starts the existing session engine for today's work, in-progress work, or a selected surah.
- Phase 2I.1 memorization overview cards plus save semantics/rating follow-up are implemented and hardware-tested: the Memorize route now has `Today's work`, `Current work`/`Recitation Focus`, and `Next up` cards backed by dashboard `todaysPlan.newMemorization`, `todayProgress`, and `upNextMemorization`, including backend-generated intra-surah review/test work for multi-day surahs. The backend cumulative schedule has been corrected to four new sessions, block cumulative, four new sessions, block cumulative, full cumulative, and a separate final whole-surah test. Session completion now requires a Needs Work/Good/Excellent rating before saving unioned ayah progress and today's daily-progress state. `Current work` remains a moving pointer through the daily assignment and only shows `Complete` when all of today's work is done.
- Review queue with red/orange/green priority cards, queue summary, due-today/upcoming/reviewed-today sections, no-due/completed/empty states, pull/focus refresh, and a page-image review session with sticky controls, reciter/speed settings, and 0-5 SM-2 rating submission.
- Full Quran reading shell with streamed 604 page images, RTL paging, last-page resume from `readingGoal`, manual Save Page, debounced auto-save, fixed page shortcuts, local page/surah/juz jump/search, saved-page clarity, reading target progress, and JS-only page bookmarks.

Web has that mobile still lacks:
- Phase 2I.1 hardware QA passed Apr 29: Mohammad verified on iPhone that today's assignment stays frozen with done/in-progress status after a rated save, current work advances correctly, ayah strengths show red/yellow/green, dashboard refreshes, and Needs Work -> Practice loads.
- Memorization session/settings parity: mobile now has the Pause & Save completed-to picker, Save & Leave / Leave without saving / Keep Practicing flow, Recitation Check with score card, Ready to Recite choice modal, Just Get Tested shortcut, pre-session setup, bookmark/resume clarity, View in Full Mushaf, Skip repeat / Skip ayah toolbar controls, tappable ayah end-marker -> ayah sheet, tap-tooltip word translation with WBW audio, recite-mode page auto-flip, recite-mode Show Word / Skip Word affordances, multi-page recite navigation pills, confetti / celebration overlay, persisted memorization defaults, and visible memorization settings/defaults implemented and hardware-tested. It still needs discovery polish: per-surah review-tone row treatment and a Tajweed Notes accordion.
- Discovery surface gaps: per-surah Tajweed Notes accordion driven by `surah.tajweedNotes`, and red/orange/green review-strength tone on surah rows derived from ayah-level strength (in addition to the existing status pill).
- Review parity: 2J.1 preserves completed-today rows and offers `Continue Reviewing` into the next open review set; 2J.2 persists active review days/sessions and groups completed prior days when a child reviews ahead; 2J.3 adds the web `Review A Few Together` / Review Multiple connected Mushaf batch flow. Phase 2J.1-2J.3 are hardware-tested. Mobile still lacks web flashcard mode and richer review celebration states, both triaged as post-Review-Multiple extras unless Mohammad reorders them.
- Full Quran/Mushaf parity: mobile Reading/Full Quran now uses bundled Quran.com QuranEngine 1405H/1920px Madani PNG pages with per-ayah pixel overlays, simplified reading actions, and hardware-tested 2L.0.ll UX polish for bottom contrast, cross-page playback/navigation, and sheet-view persistence; King Fahd 456x672 PNG files remain temporarily as a fallback until 2L.0.jj.c. Review Session still uses the 2L.0.dd Bayaan-styled text page renderer until a future renderer-unification slice. It still lacks custom contentFit tuning and decorative skinning, both backlogged separately.
- Richer parent/content surfaces beyond these blockers, such as custom goal editing, deeper plan settings, Progress, Learning Plan, Stories, Du'aas, and dedicated content pages, remain future work after the pre-TestFlight blockers unless Mohammad explicitly pulls them forward.
- Progress/achievements: dedicated progress page, charts/bars, earned and in-progress badges, weekly/monthly/session history, memorized surah list, review strength visualization, and daily progress bars.
- Stories and du'aas: no mobile story list/detail, dua list/detail/status, category filters, learned/practice tracking UI, morals, or discussion questions.
- Plans/lessons: no mobile age-group learning plan page, milestone page, goals page, lesson flow, or surah-detail learning page. Tajweed notes are present on web but remain a backlog-only item for mobile beyond the per-surah accordion called out for Phase 2I.4.
- Visual system: web feels richer through patterned headers, compact stats, color-coded cards, badges, horizontal filters, achievement progress bars, and a clear bottom nav. Mobile is functional but thin and mostly white/gray cards.

Highest-risk missing items before TestFlight:
- Phase 2I Memorization parity completion is hardware-tested through 2I.4. This includes overview cards/save semantics, Pause & Save, Save & Leave, Recitation Check, Ready to Recite, Just Get Tested, setup, bookmark/resume, View in Full Mushaf, skip controls, ayah sheet, tapped-word tooltip/WBW audio, recite page auto-flip, Show Word / Skip Word, multi-page recite navigation, confetti, settings/defaults, discovery review tone/Tajweed Notes accordion, `Next up` contiguous behavior, and Today's/Current work freeze semantics.
- Phase 2J Review parity is hardware-tested through 2J.3. Flashcard mode and richer review celebration states are documented as extras unless Mohammad reorders them.
- Phase 2K Full Quran/Mushaf Bayaan parity is required before TestFlight: the current page-image reader is usable, but it lacks the main reader tools that came from Bayaan in the web app.
- Phase 2G.4 fixed the child-du'a update bug and refreshed OpenAPI/generated clients for future mobile surfaces; authenticated production API smoke is still pending.
- Progress, Stories, Du'aas, Plan, achievements, and richer dedicated destination pages are still absent, but they are behind the three explicit pre-TestFlight blockers above unless Mohammad reorders them.
- Mobile du'a status UI is still absent; when it is built, test toggling multiple du'as independently against the Phase 2G.4 route fix.
- Future API changes must keep `lib/api-spec/openapi.yaml` and Orval outputs in sync; never edit generated files manually.

### Area-by-area parity notes

1. Dashboard/child home: mobile has three work cards plus streak/points; web adds goals, achievements, stats, today story/dua, next surah, richer quick actions, and bottom nav.
2. Memorization: mobile has the core learner, 2H.6 discovery, hardware-tested 2I.1 overview cards/save semantics, hardware-tested 2I.2a Pause & Save completed-to picking, hardware-tested 2I.2b leave flows, hardware-tested 2I.2c Recitation Check, hardware-tested 2I.2d Ready to Recite, hardware-tested 2I.2e Just Get Tested, hardware-tested 2I.2f Pre-session setup, hardware-tested 2I.2g bookmark/resume, hardware-tested 2I.2h View in Full Mushaf, hardware-tested 2I.2i Skip repeat / Skip ayah, hardware-tested 2I.2j tappable ayah end-marker sheet, hardware-tested 2I.2k tap-tooltip translation with WBW audio, hardware-tested 2I.2l recite page auto-flip, hardware-tested 2I.2m Show Word / Skip Word with real blur, hardware-tested 2I.2n multi-page recite navigation pills, hardware-tested 2I.2o confetti / celebration overlay, hardware-tested 2I.3 memorization settings/defaults, hardware-tested `Next up` skip-gap behavior, and hardware-tested Today's/Current work freeze semantics. Intra-surah review for multi-day surahs is scheduled by the backend and surfaced in Memorize as `Recitation Focus`/`Recitation` cards using the corrected cadence: four new sessions, block cumulative, four new sessions, block cumulative, full cumulative, then repeat, with the final full-surah test always separate and a partial cumulative before the final test only when 2+ leftover new sessions require it. Web still defines the required remaining work: discovery needs Tajweed Notes accordion and review-tone per surah row. Do not touch recite matcher behavior. Do not pursue tajweed coloring beyond documenting it as backlogged.
3. Review: mobile has queue summary, due/upcoming/reviewed sections, page-image session, reciter/speed controls, sticky controls, SM-2 rating, active local review day persistence, completed prior-day grouping, next-open-day continuation, and 2J.3 `Review A Few Together` / Review Multiple connected Mushaf batch review. Web still adds flashcards and richer review celebration states, now triaged as extras unless Mohammad reorders them.
4. Reading/full Quran: mobile has image paging, progress save, page/surah/juz jump/search, saved-page clarity, target progress, basic controls, and page bookmarks; web still adds the Bayaan-style tool layer: recent reads, highlights, notes, translation/tafsir/word tools, audio range playback, memorization handoff, and recite/select modes. This is pre-TestFlight.
5. Settings/targets/parent controls: mobile now has a parent Settings surface for targets, practice minutes, visibility flags, profile entry, profile creation/deletion, and JS-only memorization defaults; web still has deeper custom-goal and content-management surfaces, and persists cumulative review / review repeat count which mobile does not.
6. Progress/streaks/goals/achievements: mobile surfaces streak/points only; web has a full progress page, chart, badges, session history, goals, memorized counts, and dashboard widgets.
7. Stories/du'aas: mobile has no routes; web has category filters, detail pages, learned status, practice counts, morals, and discussion questions.
8. Plans/lessons: mobile lacks plan/lesson/surah-detail flows; web has age-group plan, milestones, weekly goals, goals reset, and surah detail content.
9. Navigation/IA: mobile needs a child-level nav shell or More screen. Suggested first pass: Dashboard, Memorization, Review, More, with More exposing Reading, Progress, Plan, Stories, Du'aas, Settings/Targets.
10. Visual/layout parity: port the web's working-app richness, not a landing page. Favor dense but warm parent-usable cards, compact stats, kid-friendly color, clear sections, and no marketing hero layout.

### Prioritized mobile roadmap

#### Phase 2G — Diagnostic cleanup + web parity foundation

Pre-TestFlight: yes.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2G.1 Diagnostic cleanup | Complete Apr 29: removed visible dashboard diagnostic panel and noisy `[noor-api]` logs while keeping readable errors/fallbacks. | `artifacts/noor-mobile/src/lib/api.ts`, `app/child/[childId]/index.tsx` | None | JS-only | Local typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2G.2 Mobile IA shell | Complete Apr 29: first-pass bottom nav and More screen so existing and upcoming pages are discoverable. | `app/child/[childId]/_layout.tsx`, `index.tsx`, `review.tsx`, `more.tsx`, `src/components/child-bottom-nav.tsx` | `GET /children/:id`; existing review/dashboard queue data for badge | JS-only | Local typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2G.3 Shared screen primitives | Complete Apr 29: reusable header/container/scroll/list/pill/loading/empty/error primitives adopted on More and Review. | `src/components/screen-primitives.tsx`, `more.tsx`, `review.tsx` | None | JS-only | Local typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2G.4 API parity checklist | Implemented locally Apr 29: fixed child-du'a keying, aligned OpenAPI for dashboard/review/progress/goals/child targets, regenerated Orval outputs. | `lib/api-spec/openapi.yaml`, `artifacts/api-server/src/routes/children.ts`, `artifacts/api-server/src/routes/sessions.ts`, generated output via Orval | Dua status bug, `reviewedToday`, dashboard/reading/progress fields | JS/TS; no native | Codegen, libs typecheck, API-server typecheck, mobile tsc passed; root typecheck blocked by unrelated frozen/reference UI React type errors; production API smoke pending |

#### Phase 2H — Must-have mobile parity before TestFlight

Pre-TestFlight: yes, unless Mohammad explicitly narrows beta to existing seeded profiles.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2H.1 Mobile onboarding/profile management | Complete Apr 29: parents can create/edit/delete child profiles on mobile, including prior memorized surahs, From/To surah range selection, initial strength/known ayahs, daily targets, practice minutes, hide stories/duas, sticky save, and keyboard dismissal. | `app/index.tsx`, `app/profile/new.tsx`, `app/child/[childId]/profile.tsx`, `src/components/child-profile-form.tsx`, `src/lib/api.ts` | Existing `POST/PUT/DELETE /children`, `GET /surahs`; no generated edits | JS-only | Local mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2H.2 Dashboard parity content | Complete Apr 29: mobile dashboard now shows API-backed stats, goals, achievements, story/du'a suggestions respecting visibility flags, next/up-next work, richer completed/empty states, and quick actions. | `app/child/[childId]/index.tsx` | Existing `GET /dashboard`; no backend/spec changes | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2H.3 Settings/targets convergence | Complete Apr 29: converged Targets into parent Settings with practice minutes, visibility toggles, child profile entry, daily target bundles/fine tuning, JS-only memorization defaults, and immediate profile preference persistence. | `targets.tsx`, `src/lib/settings.ts`, `memorization.tsx`, dashboard/More labels | Existing `PUT /children/:id`; AsyncStorage for session defaults | JS-only; no native | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2H.4 Review essentials parity | Complete Apr 29: added queue summary, upcoming/reviewed/completed/no-due states, pull/focus refresh, sticky review-session controls, reciter/speed controls, and queue/dashboard refresh path after submit. | `review.tsx`, `review-session.tsx`, `src/components/screen-primitives.tsx` | Existing reviews endpoints; no backend/spec/generated changes | JS-only | Local mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2H.5 Reading essentials parity | Complete Apr 29: added page/surah/juz jump/search, saved-page resume clarity, reading target progress, better footer/header controls, and JS-only page bookmarks while preserving the page-image reader. | `mushaf.tsx`, `src/lib/mushaf.ts` | Existing `POST /reading-progress`, `GET /dashboard`; no backend/spec/generated edits | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |
| 2H.6 Memorization discovery parity | Complete Apr 29: mobile Memorize now has current/today work, resume cards, surah search/filter, progress/strength, and starts the existing session engine. | `memorization.tsx`, `src/lib/memorization.ts`, `src/lib/quran.ts` | Existing `GET /surahs`, `GET /children/:id/memorization`, `GET /dashboard`; no backend/spec/generated edits | JS-only; no native | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed |

#### Phase 2I — Memorization parity completion

Pre-TestFlight: yes. This phase corrects the gap Mohammad called out after 2H.6 QA. Use the frozen web implementation in `artifacts/noor-path/src/pages/memorization.tsx`, `artifacts/noor-path/src/pages/quran-memorize.tsx`, `artifacts/noor-path/src/pages/settings.tsx`, and `artifacts/noor-path/src/hooks/use-settings.ts` as the reference. Preserve the existing mobile session engine; wrap and extend it instead of rewriting it.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2I.1 Memorization overview cards + save semantics | Complete Apr 29: added top three mobile cards for `Today's work`, `Current work`/`Recitation Focus`, and `Next up`; added rating-before-save with Needs Work/Good/Excellent; saves unioned ayah progress with full-surah-based status; posts daily progress for today's target so the same frozen daily assignment stays visible; `Current work` keeps advancing through the unfinished items inside today's assignment and only shows complete after all of today's work is done. Intra-surah review/test days for multi-day surahs are generated by the backend with the corrected cadence: four new sessions, block cumulative, four new sessions, block cumulative, full cumulative, and a separate final whole-surah test. | `app/child/[childId]/memorization.tsx`, `src/lib/memorization.ts`, `artifacts/api-server/src/lib/memorization-workflow.ts` | Existing `GET /dashboard`, `GET /memorization`, `GET /surahs`, `POST /daily-progress`, `POST /memorization`; no generated edits | JS-only + backend scheduler | Focused scheduler smoke check, mobile typecheck, `git diff --check`, and Mohammad hardware QA passed |
| 2I.2 Pause/save and session action parity (broad row, kept) | Add web-style Pause & Save, Save & Leave, Leave without saving, clearer review-only Finish Recitation affordances, and View in Full Mushaf from the memorization session. Preserve ayah/page modes, audio highlighting, cumulative review, reciter/speed settings, blind/blur modes, translation popup, recite mode, and mark-complete persistence. | `memorization.tsx`, maybe `mushaf.tsx` route params | Existing progress/daily-progress routes; no backend/spec changes unless a missing field is proven necessary | Medium session-state risk | Pause midway, choose completed-to ayah, go to recitation check/save, leave with/without saving, jump to Full Quran page and back |
| 2I.2a Pause & Save modal with completed-to ayah picker | Complete Apr 29: the session action opens Pause & Save; normal sessions ask "How far did you get?" with a bounded completed-to ayah stepper and Current ayah / Full range shortcuts; review-only sessions use an all-or-nothing Finish Recitation path with `Go to Recitation Check`; the existing rating/save semantics now rate only ayahs through the selected completed-to value and daily progress uses the selected completed-to ayah. | `memorization.tsx` | Existing memorization/daily-progress routes | Medium session-state risk | Local mobile typecheck, `git diff --check`, and Mohammad hardware QA passed |
| 2I.2b Save & Leave / Leave without saving / Keep practicing modal | Complete Apr 29: active-session Back opens a leave confirmation sheet; `usePreventRemove` intercepts route-removal/back gestures where supported; `Save & Leave` routes into the existing Pause & Save completed-to/rating flow; review-only remains all-or-nothing; `Leave without saving` stops recite/audio, unloads sound, clears active session state, and returns to Memorize overview without progress POSTs; `Keep Practicing` dismisses only. | `memorization.tsx` | None | Medium navigation risk | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed |
| 2I.2c Recitation Check phase with score card | Complete Apr 29: mobile now shows a dedicated full-screen Recitation Check surface after Pause & Save / Save & Leave completed-to selection, review-only Finish Recitation, normal session-complete, and recite-mode completion. It preserves Needs Work / Good / Excellent quality values `2 / 4 / 5`, normal completed-to save semantics, review-only all-or-nothing saves, and shows a Recite to NoorPath score card when entered from recite mode. | `memorization.tsx` | Existing memorization/daily-progress routes | Medium state risk | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed |
| 2I.2d "Ready to Recite?" choice modal at session end | Complete Apr 29: at session-complete (last verse done, optional cumulative review done), mobile shows a `Ready to Recite?` sheet. `Recite to Teacher` jumps straight to the existing Recitation Check surface with source `teacher`; `Recite to NoorPath` starts existing recite mode from the assigned first ayah and routes back to Recitation Check with the score card when recitation finishes. | `memorization.tsx` | None | JS-only, medium UX risk | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed |
| 2I.2e "Just Get Tested" shortcut for review-only sessions | Complete Apr 29: review-only overview assignments expose a `Just Get Tested` shortcut that skips memorization playback and routes directly to the existing Recitation Check surface for the full review-only assignment range. | `memorization.tsx` | None | JS-only | Mobile typecheck and `git diff --check` passed; Mohammad hardware QA passed after `e654ceb` |
| 2I.2f Pre-session setup phase | Complete Apr 29: overview/resume/surah starts now open setup before playback. Setup includes bounded From/To controls, repeat count 1-10, auto-advance, cumulative review, review repeat count shown only when cumulative is on, and surah previous/next plus picker navigation. Review-only assignments keep their assigned range locked and keep `Just Get Tested`; the direct review-only shortcut still skips playback and opens Recitation Check. | `memorization.tsx`, `targets.tsx` label copy | None | JS-only, medium state risk | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed after `dc7bf14` |
| 2I.2g Bookmark / resume per session | Complete Apr 29: per-child session bookmarks now persist to AsyncStorage as active session state changes with surah, From/To, current ayah, repeat count, auto-advance, cumulative review, review repeat count, review-only flag, page range, and `savedAt`. Memorize discovery shows a distinct `Resume where you left off` card that hydrates the bookmark and starts at the saved ayah. Successful saves clear the bookmark, `Leave without saving` discards it, and newer sessions replace it. | `memorization.tsx`, `src/lib/settings.ts` | None | JS-only | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed after `b152e27` |
| 2I.2h View in Full Mushaf action from inside the session | Complete Apr 30: active sessions expose `View in Full Mushaf` in the controls island, pause playback/auto-advance before navigation, push the Full Quran reader with active page/surah/ayah context, and keep normal Back returning to the mounted memorization session. | `memorization.tsx`, `mushaf.tsx` route params | None | JS-only | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed after `36622e5` |
| 2I.2i Skip repeat / Skip ayah toolbar controls | Complete Apr 30: active sessions show `Skip Repeat` and `Skip Ayah` under Prev/Play/Next. Single-phase `Skip Repeat` advances repeat passes or finishes the current ayah's repeat cycle; cumulative `Skip Repeat` advances to the next cumulative pass or exits after the final pass. Single-phase `Skip Ayah` advances to the next ayah or existing completion flow; cumulative `Skip Ayah` exits/advances review without corrupting the single-phase cursor. | `memorization.tsx` | None | JS-only, medium state risk | Mobile typecheck, `git diff --check`, and Mohammad hardware QA passed after `3bcb7b0` |
| 2I.2j Tappable ayah end-marker → ayah sheet | Complete Apr 30: the ayah-mode marker and page-mode end markers open a focused Ayah actions sheet when not in recite mode and not hidden by blind mode. Actions are limited to `Practice from this ayah` and `View in Full Mushaf` at that ayah. | `memorization.tsx` | None | JS-only | Mobile typecheck and `git diff --check` passed; Mohammad hardware QA passed after `5826666` |
| 2I.2k Tap-tooltip word translation + WBW audio button | Complete Apr 30: active-session word taps now show a compact tooltip with Arabic word, English translation/fallback, word position, and a WBW audio play button using `https://audio.qurancdn.com/wbw/{surah}_{verse}_{position}.mp3` (zero-padded). Ayah-mode taps still seek to the word; page-mode tooltip support is scoped to the active verse while non-active in-range page words keep seek behavior. | `memorization.tsx`, `src/lib/audio.ts`, `src/lib/quran.ts` | Quran.com WBW audio URLs | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `b7fcdd9` |
| 2I.2l Recite-mode page auto-flip across page boundaries | Complete Apr 30: page-mode sessions explicitly scroll the active ayah into view with short layout retries, so recite-mode verse advancement brings the next Mushaf page on screen after crossing a page boundary. | `memorization.tsx` | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `15571ae` |
| 2I.2m Recite-mode Show Word / Skip Word floating affordances | Complete Apr 30: while recite mode is active, mobile shows `Show Word` and `Skip Word` pills above the controls island. `Show Word` reveals/unblurs only the current expected word; `Skip Word` reveals/penalizes that word, advances to the next expected word or ayah, and preserves existing Recitation Check completion. Hardware QA follow-up replaced the frosted overlay with transparent text fill plus blurred text shadows for recite hidden words and page-mode blur-mode inactive words. Matcher behavior was not tightened. | `memorization.tsx` | None | JS-only, medium state risk | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `c4d06ba` |
| 2I.2n Multi-page recite navigation pills | Complete Apr 30: recite-mode Full Mushaf sessions track the displayed page, show previous/next page pills for multi-page recite ranges, and show a `Current word` jump pill when the displayed page differs from the active recite word page. Page auto-flip, Show Word / Skip Word, real blur, Recitation Check completion, matcher behavior, and tajweed backlog status are preserved. | `memorization.tsx` | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `56b2905` |
| 2I.2o Confetti / celebration overlay | Complete Apr 30: completed memorization ranges, cumulative/whole-surah recitation saves, and newly completed surahs use a JS-only native celebration overlay with falling confetti, replacing the plain success Alert for completed work. Partial Pause & Save keeps the existing simple saved Alert. | `memorization.tsx`, `src/components/celebration-overlay.tsx` | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `2226cac` |
| 2I.3 Memorization settings/defaults page (broad row, kept) | Complete Apr 30: extended existing Parent Settings with visible memorization defaults, added reciter/theme/view-mode default controls, and linked Memorize discovery/setup to the defaults surface with a `Defaults` action. | `targets.tsx`, `src/lib/settings.ts`, `memorization.tsx`, More/dashboard labels | AsyncStorage settings | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `bcfcab4` |
| 2I.3a Add cumulative review + review repeat count to persisted defaults | Complete Apr 30: `DEFAULT_SESSION_SETTINGS` and normalized AsyncStorage payload now include `cumulativeReview` and `reviewRepeatCount`; Memorize reloads defaults before opening setup for a new session while bookmark resume preserves bookmark-specific values. | `src/lib/settings.ts`, `memorization.tsx`, parent settings surface | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `bcfcab4` |
| 2I.3b Add confetti default | Complete Apr 30: `confetti` is now a persisted session default with a Parent Settings toggle, and the 2I.2o `CelebrationOverlay` only shows when the default is enabled. | `src/lib/settings.ts`, parent settings surface, `memorization.tsx` | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `bcfcab4` |
| 2I.4 Discovery polish | Complete Apr 30: Memorize discovery rows now show red/orange/green review tone mirroring web `getReviewStrengthTone`; `/api/surahs` exposes `tajweedNotes`; rows show a contained Tajweed Notes accordion where notes exist. Tajweed coloring inside the session itself stays backlogged. | `memorization.tsx` discovery section, `src/lib/memorization.ts`, `/api/surahs`, OpenAPI/generated outputs | Existing `/memorization` strength data + `tajweedNotes` on surah summary | JS + API summary shape | Mobile typecheck, API-server typecheck, `git diff --check`, and Mohammad hardware QA passed after `8674e45` |

#### Phase 2J — Review never-empty/ahead-day parity + Review Multiple

Pre-TestFlight: yes. The Review page should never strand the user with nothing to do. Use `artifacts/noor-path/src/pages/review.tsx` as the reference for `activeLocalDate`, completed-day sections, `reviewedToday`, daily-progress sync, and `Continue Reviewing`.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2J.1 Review completed-today and continue-next | Complete Apr 30: Review keeps an active review date, preserves API `reviewedToday` rows/sections, and offers `Continue Reviewing` from complete/empty states into the next open review day using existing upcoming data. Review session submits keep SM-2 behavior intact while carrying the active local date through `x-local-date`. | `app/child/[childId]/review.tsx`, `app/child/[childId]/review-session.tsx`, `src/lib/reviews.ts`, `src/lib/api.ts` | Existing `GET /reviews`, `reviewedToday`, `upcoming`, `x-local-date`; no backend changes | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `949fa3b` |
| 2J.2 Ahead-day local session behavior | Complete Apr 30: added web-like active local date/session persistence so if the child reviews ahead, completed prior days are grouped and the current active day stays understandable. Existing review-session route and SM-2 submission stayed intact. | `review.tsx` | Existing review endpoints and `x-local-date`; no backend/API changes | Medium state/refresh risk | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `06c11ec` |
| 2J.3 Review Multiple / extras triage | Complete Apr 30: mobile now ports web `Review A Few Together` with connected pending-card selection, `Select All`/`Clear All`, `Start Mushaf`, batch queue position/next-surah context, rating that advances the queue, and `Skip This Surah` keeping the skipped item pending. Flashcard mode and richer review celebration states were triaged as post-Review-Multiple extras unless Mohammad reorders them. | `review.tsx`, `review-session.tsx` | Existing review endpoints; no backend/API changes | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `ae74d1d` |

#### Phase 2K — Full Quran/Mushaf Bayaan parity

Pre-TestFlight: yes. The current mobile Full Quran page-image reader is not enough for beta because the web Mushaf reader carries the Bayaan-derived feature set. Use `artifacts/noor-path/src/pages/mushaf-reader.tsx` and `artifacts/noor-path/src/components/mushaf/bayaan/*` as the reference. Do not add native dependencies without explicit approval/rebuild note.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2K.1 Mushaf tool shell | Complete Apr 30: added a mobile tool layer over the existing page-image reader with collapsible chrome, page/surah/juz/bookmark/recent-read jump, recent reads persistence, blind mode, select mode, recite pick entry points, and ayah sheet trigger. Existing page-image paging/progress save stayed intact. | `mushaf.tsx` | Existing dashboard/reading-progress, Quran.com fetchers | JS-only medium UI/state risk | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `342204b` |
| 2K.2 Ayah sheet and annotations | Complete Apr 30: mobile ayah actions now include bookmark/highlight/note, persisted per-child annotations, selected ayah metadata, previous/next ayah navigation, annotation chips in the ayah list, ayah bookmarks in Jump, Quran.com translation/tafseer/WBW panels, native Share, and preserved memorization handoff/open-range behavior. Copy was not added because no clipboard dependency is installed; audio play/repeat/range-recitation stays in 2K.3. | `mushaf.tsx`, AsyncStorage | Quran.com translation/tafsir/WBW fetchers; existing memorization routes | Medium network/state risk | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `a931258` |
| 2K.3 Audio range/recite/select-to-memorize | Complete Apr 30: added Full Quran page/ayah/selected audio range playback, persisted reciter/speed/per-ayah-repeat/range-repeat settings, ayah-sheet and selection audio actions, recite-from-page handoff into the existing memorization matcher without tightening it, and selected/single ayah mark-memorized actions using existing memorization updates with conservative same-day daily-progress sync. | `mushaf.tsx`, `memorization.tsx`, existing audio/recite/memorization libs | Existing memorization/daily-progress routes; Quran audio URLs | Higher complexity; no matcher tightening | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `48d52a9` |

#### Phase 2L — Polish + TestFlight readiness

Pre-TestFlight: yes, but only after 2J/2K are complete and hardware-tested. Phase 2I is hardware-tested through 2I.4.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2L.1 Visual polish pass | Complete Apr 30: applied cohesive visual polish across onboarding/profile management, dashboard, More/settings/targets, Memorize, Review, Full Quran/Mushaf, bottom nav, and shared primitives while preserving 2I/2J/2K behavior. | All touched mobile screens, shared components | None | JS-only | Mobile typecheck + `git diff --check` passed; Mohammad hardware QA passed after `55b7cc7` |
| 2L.0 UI refinement + backend QA pass | Active Apr 30: open-ended pass for Mohammad's screen-by-screen UI change list and backend behavior QA fixes before app shell/assets. | TBD per reported slice | TBD; API typecheck required if backend touched | Open-ended; slice-by-slice | Awaiting Mohammad's change list |
| 2L.2 App shell/assets | Final app icon/splash, bundle/version config, production API env, no temporary dev copy. | `app.json`, assets, EAS config | None | App config changes may require EAS rebuild | Install production build, launch smoke |
| 2L.3 TestFlight build/QA | Production EAS build, App Store Connect/TestFlight setup, QA matrix, beta notes. | `artifacts/noor-mobile/*`, docs | Backend production stability | Requires production build | Login, onboarding, dashboard, memorize, recite, review, read, settings, content, offline/error paths |

#### Phase 2M — Rich learning/content pages

Pre-TestFlight: deferred unless Mohammad explicitly reorders. These used to be labeled 2I/2J before the Apr 29 correction, but the immediate beta blockers are now Memorization, Review, and Mushaf parity.

| Slice | Goal | Likely files | Backend/API deps | Risk | QA |
|---|---|---|---|---|---|
| 2M.1 Stories | Mobile stories list/detail with categories, age filtering, morals, discussion questions, and dashboard/More entry points respecting `hideStories`. | New `stories.tsx`, `story-detail.tsx`, More/dashboard links | `GET /stories`, `GET /stories/:id`, child hide flag | JS-only | Category filter, detail, hidden state |
| 2M.2 Du'aas | Mobile du'aas list/detail/status with category filter, learned toggle, practice count, Arabic/transliteration/translation/source. | New `duas.tsx`, shared content cards | Use Phase 2G.4 child-du'a route fix; OpenAPI/codegen if using generated client later | JS/backend; no native | Toggle multiple du'as independently, practice count, hidden state |
| 2M.3 Plan/goals/progress | Mobile learning plan, goals, progress page, achievements, weekly bars, memorized surah list, session history, and parent overview upgrades. | New `plan.tsx`, `progress.tsx`, `sessions.tsx`, possible parent dashboard | Dashboard, plan, goals, weekly progress, memorization, sessions endpoints | JS-only; avoid chart native deps | Empty/new child, active child, completed surahs, all age groups |
| 2M.4 Lesson/surah detail | Mobile lesson/surah detail cards with Arabic, transliteration, translation, tafsir brief, audio entry, and memorization handoff. Tajweed notes stay backlog. | New `lesson.tsx`, `surah-detail.tsx`; `src/lib/quran.ts` | `GET /surahs/:id`, Quran.com fetchers | JS-only | Open from plan/memorization; audio/translation loads |

---

## ✅ DONE — Infrastructure

- Apple Developer Program approved + App Store Connect access granted
- Backend deployed: <https://workspaceapi-server-production-cc25.up.railway.app>
- `BETTER_AUTH_URL` set to production URL
- `PROD_ALLOWED_ORIGINS` + `PROD_TRUSTED_ORIGINS` configured for CORS / Better Auth
- `/api/healthz` returns `{"status":"ok"}` (public, mounted above `requireAuth`)
- Three secrets rotated (Neon password, Better Auth secret, Hugging Face token)
- Branch-sync note: `main` + `feature/main-working-branch` were synced at `7006124` before the Phase 2I.1 cumulative scheduler QA fix. After this fix/docs commit, both branches should be synced to the current docs HEAD. `safe-cumulative` was temporary archaeology and can be ignored.
- Validation note: Phase 2G.4 touched libs/API/mobile checks pass; full root typecheck currently stops in unrelated frozen/reference UI React type errors in `artifacts/noor-path` and `artifacts/mockup-sandbox`.
- iOS bundle identifier registered: `com.mothman.noorpath`
- EAS project created at `@mothman123/noor-mobile`
- iOS distribution certificate generated (expires Apr 27 2027)
- `eas.json` committed with `development` / `preview` / `production` profiles
- `expo-dev-client` installed (auto-added by EAS during first dev build)
- First EAS development build shipped Apr 27 2026 — installed on registered iPhone, used to validate Slice 4 + Slice 5a Sessions 1+2

---

## ✅ DONE — Rendering architecture decided

| Context | Renderer | Reason |
|---|---|---|
| **Full Mushaf reader** | Page images + ayah bounding boxes | Authentic, pixel-perfect, zero rendering code |
| **Review** | Page images + highlight overlay on reviewed ayah range | Same visual the child memorized from; no layout work |
| **Memorization mode** | RN `<Text>` per word + Amiri Quran font + audio-synced highlight | Word-level granularity for follow-along audio |

**Two renderers, three contexts.** Page images do double duty for Full Mushaf and Review (the latter just adds an overlay highlighting the in-scope ayahs). Memorization is the only screen that needs custom text rendering, and that's where word-level interactivity actually matters.

**Why pages images for Review (not RN Text):** the child memorizes from a specific visual layout. Testing them on a different layout (different line breaks, different positions) would hurt recall. Page images keep the visual identical to the source they memorized from.

**Why not Skia anywhere:** Skia's basic `<Text>` doesn't shape Arabic correctly (renders disconnected isolated glyphs LTR — verified on iPhone). Skia's Paragraph API would shape correctly but require 4-8 weeks of building a custom renderer. Not justified.

**Memorization font: Amiri Quran.** Validated on iPhone Apr 27 against Digital Khatt and iOS system default. Amiri renders all Quran words correctly including the shadda+vowel sequences that Digital Khatt breaks on (verses 1, 3, 5 of Al-Fatihah). 133 KB, OFL license.

---

## ✅ DONE — Phase 1A: Mobile app skeleton + auth

Commits: `1c881c1`, `bc6115c`

- ✅ `artifacts/noor-mobile/` created (Expo SDK 54, Expo Router 6, RN 0.81.5)
- ✅ Renamed to `@workspace/noor-mobile` (matches monorepo convention)
- ✅ Migrated to pnpm-only (deleted `package-lock.json`, regenerated `pnpm-lock.yaml`)
- ✅ Better Auth client wired up via `@better-auth/expo` + `expo-secure-store`
- ✅ Sign-in screen working end-to-end against Railway backend
- ✅ Sign-out works
- ✅ Session persisted in iOS Keychain via SecureStore
- ✅ Light mode forced (`userInterfaceStyle: "light"` in app.json)

---

## ✅ DONE — Phase 1B: Child profile selector + dashboard skeleton

Commits: `ba10fc9`, `de6a2ae`

- ✅ `src/lib/api.ts` — typed `apiFetch<T>` helper using native `fetch`
- ✅ Session cookie attached via `authClient.getCookie()` (RN-specific Better Auth pattern)
- ✅ Child profile selector at `app/index.tsx`
- ✅ Dashboard at `app/child/[childId]/index.tsx` — three feature cards (Memorization, Review, Reading)
- ✅ All 8 children load on iPhone

---

## 🔜 Phase 1C — optional cleanup, deferred

Settings screen, du'aas list, stories list, achievements/progress. Low-priority; skipped to focus on memorization core. Pick up in a quiet later session.

---

## ✅ DONE — Phase 2A/B/C — page-image renderer + Reading + Review

### Phase 2A — Page image foundation

Validated and shipped in 2B (`4432f21`). Streaming from `raw.githubusercontent.com/GovarJabbar/Quran-PNG`, RTL paging via FlatList `inverted`, page-image cache via `<Image>` automatic. No bundling needed.

### Phase 2B — Full Mushaf reader (`4432f21`)

Reading card on dashboard navigates to a working Mushaf reader. 604 pages stream, RTL paging works, jump buttons (1/50/300/604), 2s debounced auto-save on swipe, last-page-read persists per child. Verified on iPhone.

### Phase 2C — Review screen (`bf8cb9a` + `08a8804` + `metro.config.js` fix)

Review queue loads with red/orange/green priority pills; surah review session shows mushaf page image + Husary audio + 0–5 quality rating modal; submit hits SM-2 backend and updates schedule. Verified on iPhone.

---

## ✅ DONE — Phase 2D — Memorization mode (Slices 1–4 + Slice 5a Sessions 1–3 local)

### Phase 2D-Core (Slice 1) — `5650d9e` + `e752721`

Single-verse focused memorization screen with Husary word-by-word audio sync.

### Phase 2D-Mushaf-Render (Slice 2a) — `1dae113`

Full Mushaf static view mode added.

### Phase 2D-Mushaf-Polish (Slice 2a-fix) — `3a9307c` + line-centering tweak

Visual repair of Slice 2a's wall-of-text rendering. Parchment page chrome ported from `noor-path` bayaan palette.

### Phase 2D-Mushaf-Sync (Slice 2b) — `ef7ae00`

Page-level audio sync + fixed controls island.

### Phase 2D-Practice (Slice 3) — `e2f9be7` + tweaks `1c89b3b`, `948de29`

Five settings + two mode buttons.

### Phase 2D-Recite (Slice 4) — `1f6557e` + docs `4100a1f`

On-device speech recognition via `expo-speech-recognition`. **Requires EAS dev build to test on hardware.**

#### Slice 4 hotfixes

- **v1 (`53675e6`)** — Audio session conflict + concurrent play race
- **v2 (`74ce890`)** — Web-derived Arabic matching + play-during-load race
- **v3 (`4b247eb`)** — Replay after natural finish + verse-boundary highlight + match logs

### Phase 2D-Polish Slice 5a Session 1 — `b73ed60` (initial) + 4 hotfixes

JS-only ship over existing EAS dev build.

**Initial commit `b73ed60`:**
- Diagnostic log cleanup from Slice 4 result handler
- `src/lib/reciters.ts` — 7-reciter table with `Reciter` type, `RECITERS` array, `findReciter`
- `src/lib/audio.ts` — `ayahAudioUrl(reciter, surah, ayah)` using `reciter.folder`
- `src/lib/memorization.ts` — initial parallel v4 chapter fetcher + `fetchTimingsForReciter` router (Husary→QDC, others→v4)
- `src/lib/mushaf-theme.ts` — 8-theme `THEMES` map, `ThemeKey`, `MushafTheme`, `THEME_DISPLAY_NAMES`, `DEFAULT_THEME_KEY`
- `memorization.tsx` — `makeThemedStyles(theme)` factory at module scope, `themedStyles = useMemo`, theme/reciter pill scrollers in settings sheet
- `src/lib/settings.ts` — initial `ChildSettings` blob persistence

**Hotfix sequence (post-hardware-testing):**

- **5a v1** — Audio session for iOS silent switch (`Audio.setAudioModeAsync({ playsInSilentModeIOS: true })`); fixed three wrong everyayah folders for Sudais/Minshawi/Ajmi (canonical names verified against everyayah.com); replaced bulk parallel v4 fetch with on-demand per-verse fetcher (`fetchQuranComV4VerseTiming` cached + dedup'd); split settings into profile-level (themeKey/reciterId/viewMode persisted) vs session-level (repeat/delay/autoplay/blur/blind reset each session). New `ChapterTimings` discriminated union type.
- **5a v2 (diagnostic)** — Added logs at `[v4-fetch]`, `[timings-effect]`, `[play]`, `[tick]` to debug missing word tracking.
- **5a v3 (`45d58a3`)** — Diagnostic logs revealed Quran.com v4 `by_ayah` endpoint returns `segments: undefined` for all non-Husary reciter IDs. Fix: fractional fallback in RAF tick (`Math.floor(frac * wordCount)`) when `segsRef.current` is empty. Mirrors web app's behavior. Plus `setVolumeAsync(1.0)` on every Sound (Afasy noticeably quieter than Husary on everyayah). Removed all diagnostic logs.
- **5a v4 (`d5d5f1f`)** — `LEAD_MS = 300` constant time-shift in fractional-fallback branch so highlight runs slightly ahead of audio (matches Husary's anticipatory feel from QDC segment 1 starting at frac=0).
- **5a v5** — Bumped `LEAD_MS = 500` after 300ms still trailed Afasy. Confirmed working for short ayahs. Lag may reappear on longer verses (Al-Baqarah, Ayat al-Kursi) — accepted limitation.

**Slice 5a Session 1 status: works very well on iPhone.** All 7 reciters play; word tracking works for all (true QDC for Husary, fractional fallback for others); audio plays through iPhone silent switch; volumes normalized; theme + reciter pickers in settings sheet; profile vs session settings split.

### Phase 2D-Polish Slice 5a Session 2 — `18f054d`

JS-only. Three features in one commit:

- **Translation popup (working)** — `word_fields=translation` + `translations=131` (Sahih International) added to both `fetchSurahVerses` and `fetchVersesByPage`. `onLongPress` (delayLongPress=400) on in-scope words opens a centered fade Modal with Arabic + English. Inner `Pressable` with empty `onPress` absorbs tap so backdrop dismiss only fires outside the card. Short tap still triggers tap-to-seek.
- **Playback rate (working)** — `PLAYBACK_RATES = [0.75, 0.85, 1.0, 1.15, 1.25, 1.5]` pill scroller in settings sheet (matches theme/reciter pattern). Skipped `@react-native-community/slider` since it has native code requiring a rebuild — discrete pill values are kid-friendlier anyway. `setRateAsync(rate, true)` (`shouldCorrectPitch=true`) called in `playVerse` after `createAsync`, plus a separate effect that pushes rate changes to the active sound mid-playback.
- **Tajweed coloring (broken — backlogged)** — created `src/lib/tajweed.ts` with 21-class `TAJWEED_COLORS` map and `extractTajweedColor` helper, added `text_uthmani_tajweed?` to `ApiWord`, included it in `word_fields` of both fetchers, wired tajweed toggle into settings sheet, applied colors via `extractTajweedColor` in both ayah and page word renderers. End markers and out-of-scope words skip coloring. **Doesn't visually color anything on hardware.** Likely cause: Quran.com v4 doesn't actually populate `text_uthmani_tajweed` as a `word_field` despite docs (the web app uses verse-level `text_uthmani_tajweed` + `splitTajweedIntoWords` parser, suggesting it's only available at the verse level). Wiring is in place; investigation deferred.

---

### Phase 2D-Polish Slice 5a Session 3 — cumulative review — `safe-cumulative` / synced

JS-only. Hardware-tested by Mohammad and synced to `main` + `feature/main-working-branch` on Apr 28.

Current local commits:
- `4599dff` — web-style cumulative review state machine (`internalPhase: "single" | "cumulative"`, `cumAyahIdx`, `cumPass`, `cumUpTo`, `playingVerseNumber`, `reviewRepeatCount`)
- `2eaad4b` — skip handling fix: Next from newest single verse enters cumulative instead of skipping review
- `34d0172` — final-verse Next starts final cumulative review instead of completing immediately
- `2147b07` — final-verse Next button enabled when cumulative review can start
- `b2b3186` — normal repeated verses show `Pass X/Y · Verse A of B` in the header

Behavior now mirrors the web retention flow:
- `cumulativeReview` toggle defaults false each screen mount.
- `reviewRepeatCount` defaults 3 and ranges 1-10.
- After each new verse finishes its normal repeats, cumulative review plays `ayahStart..currentVerse` for `reviewRepeatCount` passes.
- During cumulative review, each verse plays once (no per-verse repeats).
- Header shows `Pass X/Y · Ayahs A-B` during cumulative review.
- Header shows `Pass X/Y · Verse A of B` during normal repeated single-verse playback.
- `Next` during cumulative exits cumulative and advances/completes; `Prev` during cumulative bails back to single phase.
- `Mark Complete` remains submit + success alert; cumulative review happens during memorization, not after submit.

Hardware QA status:
- Mohammad tested the final cumulative review fixes and repeat-pass header and approved moving to 5b.
- Branches were synced afterward: `origin/main` and `origin/feature/main-working-branch` advanced to `7e56509`.

### Phase 2D-Polish Slice 5b — `expo-blur` — `aa004ff`

Goal: real blur via `expo-blur`, replacing the current opacity fallback (`styles.mushafWordBlurred { opacity: 0.35 }`) used by `blurMode` in `memorization.tsx`.

Completed and hardware-tested:
- Installed `expo-blur@~15.0.8` in `@workspace/noor-mobile` using pnpm.
- Updated `memorization.tsx` page-mode word rendering to use a `BlurView` overlay for inactive in-scope words while audio is playing.
- Removed the old opacity fallback from blurred words.
- Preserved `Pressable` handlers so tap-to-seek and long-press translation still route through the same word wrapper.
- Ran `cd artifacts/noor-mobile && npx tsc --noEmit` clean.
- EAS development build finished: `cfb3f406-5fec-405a-a150-e525a96ecff2`.
- User installed/tested the build and reported "All done."
- Metro initially showed a stale `Unable to resolve "expo-blur"` bundle error after native install; restarting `npx expo start --dev-client --clear` with the new dev build resolved the expected native-dependency refresh path.

Phase 2D is complete. Tajweed remains wired-but-not-rendering and is intentionally backlogged.

---

## ✅ DONE — Phase 2E — dashboard polish & today's-work content

Implemented, typechecked, and hardware-tested Apr 28, 2026.

- Mobile dashboard now loads `/api/children/:id/dashboard` plus `/api/children/:id/reviews`.
- Memorization, Review, and Reading cards show today's concrete queued work/status.
- Dashboard review previews and review queue cards share red/orange/green priority colors via `src/lib/review-priority.ts`.
- Review queue cards now have priority-colored rails/backgrounds, not just text pills.
- Profile selector rows now show richer child details: avatar bubble, age label, streak, points, and arrow affordance.
- No native dependencies added. Tajweed untouched.
- Local QA: `cd artifacts/noor-mobile && npx tsc --noEmit` passed clean.
- Hardware QA: Mohammad checked the profile/dashboard/review priority polish on iPhone and said it looks good.

## ✅ DONE — Phase 2F — target-setting UI

Implemented, committed, typechecked, route-fixed, and hardware-tested Apr 28, 2026.

- Added a mobile Targets screen at `app/child/[childId]/targets.tsx`.
- Parents can set memorization, review, and reading pages-per-day per child.
- Uses existing backend fields: `memorizePagePerDay`, `reviewPagesPerDay`, `readPagesPerDay`.
- Preset chips match the web reference; minus/plus controls allow fine tuning without keyboard input.
- Child dashboard has a `Targets` header action and refreshes when returning from the Targets screen.
- Shared mobile `apiFetch` now sends `x-local-date` and normalizes HTML API failures into readable messages, so transient server errors do not render raw HTML in-app.
- Child dashboard retries `/api/children/:id/dashboard` once, then falls back to child/profile plus review queue data so the dashboard remains usable and Targets stays reachable if today's plan endpoint flakes.
- No native dependencies added. Tajweed untouched.
- Local QA: `cd artifacts/noor-mobile && npx tsc --noEmit` passed clean after the target UI, after the API helper hotfix, and after the dashboard fallback.
- Production QA: authenticated production dashboard fetches for child L and Joll returned 200 after the screenshots, so the saved target values are not corrupting dashboard data.
- Hardware QA: Mohammad reported the Targets/dashboard route fix passed on iPhone; dashboard no longer gets stuck on `/api/children/targets/*`.

## ✅ DONE — Phase 2J.2 Ahead-Day Local Session Behavior

Phase 2J.2 implementation commit: `3365683` (`feat(mobile): persist ahead review sessions`); final synced docs before hardware QA: `06c11ec`. Mohammad hardware-tested Phase 2J.2 and reported QA passed. The pass clears active review date persistence after navigation/refresh, restored completed active-day rows, next-open review day continuation, grouped completed prior-day sections, existing SM-2 submission via `reviewDate`/`x-local-date`, dashboard/review refresh smoke, bottom nav review count smoke, and existing due/upcoming/reviewed sections.

## ✅ DONE — Phase 2J.3 Review Multiple

Phase 2J.3 implementation commit: `ae74d1d` (`feat(mobile): add review batch queue`). Mohammad hardware-tested Phase 2J.3 and reported QA passed.

QA-cleared behavior:
- Mobile `Review A Few Together` appears when more than one pending review remains.
- The child can select with the corner circles, expand only through neighboring pending cards, or use `Select All` / `Clear All`.
- `Start Mushaf` opens the existing review-session route as one connected batch queue.
- The batch review session shows queue position and next-surah context.
- Rating submits the current surah through existing SM-2 review submission and advances to the next selected item.
- `Skip This Surah` advances without submitting, so the skipped surah remains pending.
- Completed rows/session persistence, dashboard/review refresh, and bottom-nav count smoke passed.
- Flashcard mode and richer review celebration states were triaged as post-Review-Multiple extras unless Mohammad explicitly makes them beta blockers.

## ✅ Phase 2L.1 Visual Polish Pass — hardware-tested

Phase 2K Full Quran/Mushaf parity is hardware-tested through 2K.3. Phase 2L/TestFlight readiness is now the active pre-beta blocker. Phase 2L.1 is implemented and hardware-tested; Phase 2L.0 UI refinement + backend QA pass now runs before app shell/assets and production TestFlight work.

Phase 2L.1 implementation and QA:
- Shared primitives and bottom nav now use a lighter app shell, tighter headers, cleaner loading/empty/error states, consistent pills/rows, and elevated bottom navigation/control surfaces.
- Onboarding/profile management, dashboard, More/settings/targets, Memorize discovery/setup/session/Recitation Check, Review/review-session, and Full Quran/Mushaf received cohesive card, spacing, border, status, and scanning polish.
- Behavior was intentionally preserved: Phase 2I memorization, Phase 2J review, Phase 2K Mushaf/audio/annotations/recite/select behavior, dashboard/review refresh, bottom nav review count, paging/autosave, recite matcher behavior, and tajweed backlog status are unchanged.
- JS-only. No native dependencies. No frozen web edits.
- Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- Mohammad hardware-tested the visual polish screenshots/smoke after commit `55b7cc7` and reported QA passed.

Next action:
- Wait for Mohammad's UI change list and backend QA list, then start Phase 2L.0 — UI refinement + backend QA pass. Each reported item becomes its own Codex slice with implementation, mobile validation (`cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`), branch sync, and hardware QA. Do not start Phase 2L.2 app shell/assets until Mohammad explicitly says 2L.0 is done.

---

## 🔜 Phase 2L.0 — UI refinement + backend QA pass

Pre-TestFlight: yes. Phase 2L.1 was a single-slice cohesive polish pass; Phase 2L.0 is an open-ended phase for the specific UI tweaks and backend behavior fixes Mohammad wants before TestFlight. Mohammad will provide a list (with screenshots if helpful) covering both UI changes and backend behavior he wants fixed. Each reported item becomes its own Codex slice in priority order.

Process for each reported item:
- Confirm scope and acceptance criteria with Mohammad before coding.
- Implement as a focused slice on `main`. Keep frozen `artifacts/noor-path` untouched. Do not tighten the recite matcher. Do not add native dependencies without explicit approval. Do not touch the tajweed implementation except to document it as backlogged.
- Local validation: `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. If the change touches the API server, also run `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`.
- Commit on `main`, push `origin main`, fast-forward `feature/main-working-branch` onto `main`, push `origin/feature/main-working-branch`, switch back to `main`. All four refs synced after every commit.
- Update both TODO.md and PHASE_2D_HANDOFF.md with the slice result before handing off.
- Hand back to Mohammad for hardware QA on iPhone before starting the next slice.

Phase 2L.0 is complete when Mohammad explicitly says "2L.0 is done" or equivalent. Only then does Phase 2L.2 app shell/assets start.

Reported items (filled in as Mohammad reports them):
- 2L.0.a Profile picker redesign — Implemented locally Apr 30 in implementation commit `37f199c`: replaced the bulky profile-card stack with a compact searchable, sortable, filterable picker; added parent avatar menu, per-profile gear sheet, account-settings placeholder route, and Active today strip/filter using existing mobile/API surfaces. JS-only; no backend, native dependency, tajweed, matcher, or frozen web changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test before 2L.0.b starts.
- 2L.0.a follow-up stale profile picker list — Implemented locally Apr 30 in implementation commit `f933b10`: profile picker now refreshes `/api/children` on screen focus with `useFocusEffect`, so create/edit/delete changes are visible when returning to the picker without force-quitting. JS-only; no visual, backend, native dependency, tajweed, matcher, or frozen web changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone smoke for create/back, edit/back, and delete/back.
- 2L.0.b Dashboard top bar redesign + child switcher + date scrubber — Implemented locally Apr 30 in implementation commit `13852e0` (`13852e0c3239d29ff1720713b9c2205a4a1a2099`): replaced the old dashboard header and black Today banner with compact top chrome, tappable avatar/name child switcher, gear-to-settings action, slim infinite date scrubber, Today reset, and read-only non-today work cards; `GET /children/:childId/dashboard?preview=true` now skips daily-progress insert/backfill and returns synthesized not-started progress when no row exists. No frozen web files, native dependencies, tajweed implementation, or matcher behavior were touched. Local validation passed with API-server typecheck, mobile `npx tsc --noEmit`, and `git diff --check`. Hardware QA: pending Mohammad iPhone test, including DB row-count check for future scrubs.
- 2L.0.c Dashboard body reorganization — Implemented locally May 1 in implementation commit `277bb5e` (`277bb5effdef51000d007f902fe1569b416bc702`): fixed the redundant date scrubber Today/Today state, replaced the three large stats cards with one slim inline banner, promoted the `Today's Work` heading, moved Story/Du'a into a compact post-task pair respecting hide flags, combined Goals/Achievements into a tabbed card, and reduced Quick Actions to Plan, Progress, and More. JS-only; no backend, native dependency, frozen web, tajweed, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.d Mobile Progress page + week/month range backend — Implemented locally May 1 in implementation commit `78d2705` (`78d2705d45dc4a1c708171c23357f5f77ebcb68d`): added `/child/[childId]/progress` in the active mobile app with compact dashboard-style top chrome, 2x2 stat cards, hand-rolled Week/Month activity bars, earned/in-progress achievements, More/Quick Actions navigation, and removed the redundant slim dashboard stats banner. Extended `GET /api/children/:childId/weekly-progress` with `?range=week|month`, zero-filled daily entries, aggregated completion fields, `totalActivityScore`, and refreshed OpenAPI/Orval outputs. The dashboard endpoint's `memorizationStats.weeklyProgress` remains the known placeholder array; mobile Progress intentionally fetches the dedicated `/weekly-progress?range=...` endpoint instead. No frozen web files, native dependencies, tajweed implementation, or matcher behavior were touched. Local validation passed with `/Users/mothmanaurascape.ai/Library/pnpm/pnpm --filter @workspace/api-server run typecheck`, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.d follow-up Progress route + Plan disable — Implemented locally May 1 in implementation commit `961ac36` (`961ac3634b01dbb261a094330429c5b94473f8dc`): dashboard Progress touchpoints now use the typed `/child/[childId]/progress` pathname/params navigation, including the Achievements tab `View all →` link, so hardware should no longer see a page-not-found route. The Plan Quick Actions tile and Goals tab `View all goals →` touchpoint are now disabled/greyed with `Coming soon` pending slice 2L.0.e instead of navigating to the missing `/plan` route. JS-only; no backend, native dependency, frozen web, tajweed, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.e Mobile Plan page + dashboard Plan touchpoints — Implemented locally May 1 in implementation commit `340ab81` (`340ab81c5d85b8a69f6f1c972164c3760e487698`): added `/child/[childId]/plan` in the active mobile app with Progress-style top chrome, age-group label header, practice-minutes badge, goals card with progress and reset-to-auto confirmation via `PUT /api/children/:childId/goals`, narrative description, current phase, weekly goals 2x2 grid, milestones, and tajweed-rules text from the existing plan endpoint. Re-enabled the dashboard Quick Actions `Plan` tile and Goals tab `View all goals →` link so both route to the new Plan page via typed pathname/params, and removed the previous disabled/Coming soon dashboard helper styles. JS-only; no backend, native dependency, frozen web, tajweed implementation, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. This completes the Plan/Progress dashboard wiring originally scoped in slices 2L.0.c through 2L.0.e. Hardware QA: pending Mohammad iPhone test.
- 2L.0.f Memorize discovery average strength — Implemented locally May 1 in implementation commit `6dc3ee4` (`6dc3ee4d149beae94c8122a068d0c7bed1bae3c8`): the mobile Memorize discovery progress card now calculates `Average strength X/5` from qualifying memorization rows only (`memorized` / `in_progress`, or rows with memorized ayahs/verses), excluding `not_started` / unstarted rows from the mean. When no qualifying rows exist, the average-strength line and its summary dots are hidden instead of falling back to `1/5`; individual surah-row strength labels/dots, search/filter behavior, rows, data fetching, and backend behavior are unchanged. JS-only; no backend, native dependency, frozen web, tajweed, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.g Memorize session bottom bar redesign — Implemented locally May 1 in implementation commit `4345d63` (`4345d63a90e692b465af67ba56d42faf8653112b`): the active mobile memorization session now has one compact bottom playback row with icon-only Skip Repeat, Prev, primary Play/Pause, Next, and Skip Ayah controls wired to the existing handlers; the always-visible Ayah by Ayah / Full Mushaf toggle, Blind/Recite toggles, View in Full Mushaf action, and Pause & Save entry point moved into the top Session section of the existing settings sheet. Pause & Save still opens the existing completed-to-ayah / recitation-check flow, View in Full Mushaf still uses the existing memorization-to-Mushaf route behavior, and recite-mode assist controls remain unchanged. JS-only; no backend, native dependency, frozen web, tajweed implementation, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.h Memorize ayah-by-ayah view redesign — Implemented locally May 1 in implementation commit `ebd56b7` (`ebd56b7de494cba185fb9ca9a607bb9f16908ab3`): ayah-by-ayah session mode now renders a FlatList over the full session range with verse-key separators, full-opacity active ayah, dimmed context ayahs, active-word highlighting/tap-to-seek/long-press translation preserved, tap-to-jump for non-active rows, active-ayah auto-scroll, and a per-session Show translation toggle with inline Sahih International translations and styled translator insertions. The per-ayah 3-dot sheet now offers Listen, Translation, Bookmark, View in Full Mushaf, and Copy; bookmarks reuse the Full Quran reader's ayah annotation store, Copy uses React Native's built-in Clipboard fallback, and Full Mushaf/page mode plus bottom bar/settings/audio/recite/cumulative/save flows remain on the existing paths. JS-only; no backend, native dependency, frozen web, tajweed implementation, or matcher changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.h follow-up translation source fix — Implemented locally May 1 in implementation commit `7cc46d4` (`7cc46d47571b5b4c1fe1e1f1c528de6149464424`): the ayah-by-ayah inline translation toggle now uses the already-fetched `fetchSurahVerses` verse data (`translations=131`) instead of making per-row Quran.com by-key API calls, with shared `cleanTranslationHtml` cleanup and the existing styled insertion rendering preserved. The per-ayah Translation action sheet also prefers the already-loaded verse translation and only falls back to `fetchAyahTranslation` if the loaded translation is missing. JS-only; no backend, native dependency, frozen web, tajweed implementation, audio engine, matcher, cumulative review, or save/leave flow changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.h second follow-up shared reading-mode translation fetcher — Implemented locally May 1 in implementation commit `891231b` (`891231bf4cd26d57ca93df5a549527706d794008`): abandoned the unreliable inline `verse.translations` data path for memorization translations and moved both Reading/Mushaf and Memorization surfaces onto the shared `fetchAyahTranslation` helper in `src/lib/quran.ts`. The helper now uses the reading-mode-proven Quran.com by-key endpoint with translation ID `20` instead of `131`, keeps shared HTML cleanup/cache behavior, and drives both memorization inline translations and the per-ayah Translation modal. JS-only; no backend, native dependency, frozen web, tajweed implementation, audio engine, matcher, cumulative review, save/leave flow, or reader feature changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.i Memorize session Full Mushaf paging — Implemented locally May 1 in implementation commit `9b778af` (`9b778af5ad76cc67f78d38670f1fd718f97e4c1f`): Full Mushaf session mode now renders one fitted page per horizontal `FlatList` swipe using the reading-mode inverted/paging pattern, with each page containing its own vertical `ScrollView`, the existing Arabic/word/end-marker rendering, blind/blur and recite word masking, word highlight/tap/long-press behavior, and a subtle multi-page `Page X of Y` pill with bounded chevrons. Active verse and cumulative `playingVerseNumber` changes programmatically flip to the right page and scroll the active line to roughly 30% from the page viewport top; recite-mode expected-word changes also reuse the page lookup for auto-flip while keeping Show Word / Skip Word visible. Ayah-by-ayah mode, audio engine, matcher, backend interactions, settings/save/leave/check/celebration flows, and frozen web files were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. This completes Round 1 memorization items (`2L.0.f`, `2L.0.g`, `2L.0.h`, `2L.0.i`); Round 1 also includes dashboard slices `2L.0.a` through `2L.0.e`, already done. Hardware QA: pending Mohammad iPhone test.
- 2L.0.j Memorize session quick Blind/Recite toggles + no-speech suppression — Implemented locally May 1 in implementation commit `cb0712b` (`cb0712bc35ed5c040dd92cdd4b1050d4d18415d9`): added always-visible Blind and Recite icon toggles to the in-session top chrome before the settings gear, both wired to the same handlers as the settings-sheet switches with active primary-blue state; Recite-off now clears listening/error state and stops speech recognition through the shared handler. The recite speech error handler now suppresses silent `no-speech` / `no-match` recognition errors without changing user-facing UI state while preserving all other surfaced recite errors. JS-only; no backend, native dependency, frozen web, matcher, page/ayah rendering, audio engine, cumulative review, or save/leave flow changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.k Memorize recite mode all-ayah blur + warm-up watchdog — Implemented locally May 1 in implementation commit `0a6198a` (`0a6198a389192791a91f532330bb89c08d938dda`): ayah-by-ayah recite mode now applies the existing word-blur treatment to every in-scope non-active ayah while preserving active-ayah reveal as the matcher advances; non-active dimming is suppressed while recite blur is active. Recite startup now tracks first transcript delivery and, if no transcript arrives after about 3 seconds while matcher progress is still zero, silently stops, waits about 200ms, and restarts recognition with the same options, capped at two retries. Full Mushaf recite rendering, matcher criteria, audio engine, cumulative review, settings/save/leave, Show Word / Skip Word, no-speech suppression, frozen web, backend, native dependencies, and tajweed were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.k second follow-up recite first-ayah diagnostic logs — Implemented locally May 1: temporary recite console diagnostics now trace top-chrome/settings recite toggle entry, enter-recitemode setup snapshots, speech-recognition start options/call/resolve/reject, warm-up watchdog set/fire/clear/restart, every transcript event and matcher outcome, `wordMatches` predicate input/output, active-verse reset snapshots, and ayah-list tap-to-jump changes. Diagnostic-only; no matcher acceptance, rendering, audio, backend, frozen web, native dependency, or tajweed behavior changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone repro and recite console capture.
- 2L.0.k third follow-up remove broken recite warm-up watchdog — Implemented locally May 1: the diagnostic logs proved the warm-up watchdog from 2L.0.k was itself causing the failure by driving an infinite speech-recognition restart loop before transcripts could arrive. The watchdog, retry counters/tokens/timers, delayed restart path, and all temporary recite diagnostics were removed entirely; recite mode now returns to a single start on enable and stop on disable with no warm-up restart loop. The blur-all-ayahs behavior from 2L.0.k is preserved. Hardware QA pending: Mohammad will verify (a) recite mode now picks up transcripts and advances the matcher, (b) no infinite restart loop, and (c) blur-all behavior still works.
- Recurring memorize session loading death spiral diagnostic — Implemented locally May 1: temporary `[noor-mem]` logs now trace `beginSession`, every `setLoading(true/false)` call site, Step 1 branch/dashboard resolution/error paths, Step 2 entry/early-return/fetch/resolve/error/cleanup paths, and throttled render loading decisions. Diagnostic-only; no session loading logic, audio, recite matcher, ayah/page rendering behavior, backend, native dependency, frozen web, or tajweed behavior changed. Hardware QA pending Mohammad repro and `[noor-mem]` console capture to identify which expected loading event does not fire.
- Recurring memorize session loading death spiral diagnostic round 2 — Implemented locally May 1: the previous diagnostic round did not capture the failing session, and Mohammad narrowed the repro to completing a session then immediately tapping `Next up`, often in Full Mushaf/page mode with the parchment card spinner visible. Added additional `[noor-mem]` diagnostics for save-completion cleanup/post-save state, `beginSession` post-reset snapshots, Full Mushaf outer paging `FlatList` render state, and page-level `renderMushafPage` attempts/bailouts when `pageWordsMap` lacks a page. Diagnostic-only; no loading/session, FlatList, audio, recite matcher, ayah/page rendering behavior, backend, native dependency, frozen web, or tajweed logic changed. Hardware QA pending Mohammad's targeted save-to-next repro and console capture.
- Recurring memorize session loading death spiral fix — Implemented locally May 1: round-2 diagnostics identified `pageWordsMap` not being repopulated when consecutive Full Mushaf memorization sessions share the same `pageStart`/`pageEnd` after `beginSession` clears the map. Fixed by adding `sessionLoadId` to the `fetchVersesByPage` page-fetch effect dependencies so the page data refetches on every new session, even when the Mushaf page range is unchanged. All three rounds of temporary memorize loading diagnostic logs are removed from the mobile screen. JS-only; no audio, recite matcher, ayah/page renderer behavior, backend, native dependency, frozen web, or tajweed logic changed. Hardware QA pending Mohammad same-page Next up repro and broader memorize regression smoke.
- 2L.0.l Memorize Full Mushaf bismillah header + orphaned end-marker wrap fix — Implemented locally May 1 in implementation commit `c45cf13` (`c45cf13295303e3fffb34927e6aa59ea63162d92`): Full Mushaf session pages now insert a centered non-ayah bismillah header after eligible surah-title banners for every surah except Al-Fatiha and At-Tawbah, without adding an ayah marker, tap target, blur, matcher state, word index, or verse-number side effect. The Full Mushaf line renderer now groups each verse's final word with its tappable end-marker as one wrapping unit so circled ayah markers cannot orphan onto their own line unless the final word wraps with them. Ayah-by-ayah mode, audio engine, recite matcher, cumulative review, save/leave flows, backend, native dependencies, frozen web files, and tajweed behavior were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.m Memorize Full Mushaf line-by-line Madani layout with per-line auto-fit — Implemented locally May 1 in implementation commit `5defbfd` (`5defbfd9274b631f86bf4cb5be4df4f8a51f812c`): Full Mushaf session pages now render one canonical row per Quran.com `line_number`, with non-wrapping RTL rows, per-line measured font auto-fit for dense lines, `space-between` word spreading for sparse lines, and synthetic surah-title / bismillah rows inserted at the canonical line positions when Quran.com omits them. Word components and ayah end markers remain individual interactive elements, preserving audio highlight, tap-to-seek, long-press translation, tapped-word tooltip/WBW audio, blind/blur, recite word masking, end-marker ayah sheet, cumulative review, and recite page auto-flip. This is the foundational refactor that makes the Full Mushaf renderer line-identical to the real Madani Mushaf; polish iteration is expected for device-specific font scaling, spacing, title positions, bismillah edge cases, and page line-count edge cases. Ayah-by-ayah mode, audio engine, recite matcher, save/leave flows, backend, native dependencies, frozen web files, and tajweed behavior were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.n Memorize Full Mushaf QUL canonical KFGQPC V4 layout — Implemented locally May 1 in implementation commit `5e839ab` (`5e839ab6792195600109a7be5e70980f10dab0fe`): Full Mushaf session pages now use bundled QUL KFGQPC V4 layout data (1441H King Fahd print) plus the matching word-index mapping to render canonical page lines instead of Quran.com `line_number` grouping. QUL now owns surah-title and basmallah rows, so the previous synthetic title/bismillah injection was removed. Existing Quran.com word objects still flow into the same word and ayah-end-marker components, preserving audio highlight, tap-to-seek, long-press translation popup, blind/blur, recite blur-all/reveal, end-marker ayah sheet, cumulative review via `playingVerseNumber`, and recite page auto-flip. JSON assets are 1.2 MB layout + 6.1 MB words, 7.6 MB combined. Ayah-by-ayah mode, audio engine, recite matcher, save/leave flows, backend, native dependencies, frozen web files, and tajweed behavior were not changed. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad printed-KFGQPC comparison and iPhone regression test.
- 2L.0.o Review queue visual hierarchy + tomorrow-only Upcoming + completion celebration — Implemented locally May 1 in implementation commit `58be00b` (`58be00bb5ca5b8e12e66cc2a064a6ba3a8388364`): the mobile Review queue now gives Due Today cards the primary full-color treatment, renders Reviewed Today cards as clearly finished with a muted gray background, softer text/status pill, and a green checkmark rail, hides the Upcoming Reviews section whenever Due Today has work, filters Upcoming locally to literal tomorrow-only items using the local tomorrow date, and updates the header Upcoming count from the same tomorrow-only filter. When today's due queue is empty and Reviewed Today has at least one item, the empty Due Today area becomes an `All caught up!` celebration card with count copy, `Start tomorrow's reviews early`, and `Done for today`; starting tomorrow early locally promotes tomorrow-due cards into the Due Today section without backend endpoint changes, and pull-to-refresh / due-card review-session navigation remain on the existing paths. JS-only; no backend, native dependency, frozen web, tajweed, matcher, or spaced-repetition algorithm changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.p Review session feature parity — Implemented locally May 1 in implementation commit `80663ff` (`80663ff86e531e7ebd84f1bd83b537b0338646e4`): removed the redundant review-range banner by moving Surah, ayah range, and page range into the header; kept the batch `Next:` pill compact above the Mushaf image; added an always-visible ayah strip with Arabic-Indic numbered circles, active-ayah highlight, auto-centering, tap-to-seek, and long-press translation popup using `fetchAyahTranslation` translation ID 20; added Review Session Swipe/Scroll Mushaf layouts; added profile-level `mushafViewMode` with `swipe` default, per-session override, and Save as default; and made the same Mushaf view preference apply to memorization Full Mushaf. JS-only; no backend, native dependency, frozen web, tajweed, matcher, or audio-engine changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.q Review queue Continue Reviewing + day-total count + diagnostic cleanup — Implemented locally May 1 in implementation commit `52925a7` (`52925a738fceebe988ee67c6606e32c48a37043e`): mobile Review now matches the frozen webapp's `Continue Reviewing →` behavior by always showing the celebration-card action when a review day is complete, advancing `activeLocalDate` to the next open review date via stored-completion walking plus earliest backend upcoming due date, and removing the local "Start tomorrow's reviews early" promotion path. The queue header now surfaces day-total progress copy such as `1 review done, 1 remaining for today`, while preserving the tomorrow-only Upcoming filter, Due/Reviewed active-date labels, Completed Review Days, AsyncStorage review sessions, backend-reviewed merge, and daily-progress sync. Removed the `[noor-review-queue]` diagnostic logs left from the 2L.0.o-debug pass. JS-only; no backend, native dependency, frozen web, review-session, tajweed, matcher, or spaced-repetition algorithm changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.r Review queue frozen daily set + persistent Continue Reviewing + Review Specific Surah — Implemented locally May 2 in implementation commit `4f65d40` (`4f65d40291e3e203a6c9282503d3c94cbe3c0a8c`): the API now persists `review_daily_set` snapshots per child/local date, returns only the frozen set's pending items for real today, moves newly surfaced overdue work to Upcoming, and uses red/orange/green plus canonical wrap order (Al-Fatiha, then 114→2) with oldest/never-reviewed fallback so caught-up memorized children still receive work. Mobile Review retired the 2J.2/2L.0.q `activeLocalDate` ahead-day navigation, Completed Review Days grouping, and "Done for today" dismissal; `activeLocalDate` is now today's local date, completion shows a static `Today's work complete!` panel, and `Continue Reviewing →` persists a per-day continue offset so repeated taps promote exactly one additional frozen future day at a time without advancing the active date. Added always-visible `Review Specific Surah` picker with memorized-surah search, canonical wrap sorting, priority dots, verse-range steppers, and ad-hoc review-session routing; ad-hoc submits pass the selected ayah range so SM-2 updates the intended range. The DB package has no migrations directory/generate script, so this is a Drizzle schema addition intended for the existing `drizzle-kit push` flow rather than a generated migration file. No frozen web files, native dependency, tajweed, recite matcher, or Orval/generated files changed. Local validation passed with `npx tsc --build lib/db`, `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.s Review queue timezone-correct reviewedToday + Upcoming gate — Implemented locally May 2 in implementation commit `6c126a5` (`6c126a5becd245f0dbb1df12ed690f7a0e86cd15`): added nullable `review_schedule.last_reviewed_local_date`, writes it from `getRequestLocalDate(req)` on every review rating, and uses it for reviewed-today matching so Pacific evening reviews no longer compare against the server's UTC-local `Date` fields. Mobile Review now renders Upcoming Reviews only when `dueToday.length === 0`, with spacing simplified to depend on Reviewed Today. Production needs Mohammad to run `DATABASE_URL='<prod URL>' pnpm --filter @workspace/db push` and accept only the add-column prompt for `last_reviewed_local_date`; abort if drizzle-kit proposes any drop/rename. No frozen web files, native dependency, tajweed, recite matcher, Orval/generated files, SM-2 algorithm, or backfill changed. Local validation passed with `npx tsc --build lib/db`, `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, and `git diff --check`. Hardware QA: pending Mohammad iPhone test after schema push.
- 2L.0.t Future review days computed fresh + single arrow + hidden upcoming preview — Implemented locally May 2 in implementation commit `9acea01` (`9acea016d3513452d31a719c43280b68be56b371`): future Continue Reviewing offsets now ignore old `review_daily_set` snapshots and compute fresh from live schedule due dates each fetch, while only today's set remains frozen/read/written. This fixes stale future-day freezing after SM-2 changes a due date; a red rating that sets Al-Fatiha due tomorrow can appear on the first Continue Reviewing tap. Mobile removed the duplicate Ionicons arrow from the Continue Reviewing button and removed the Upcoming Reviews card preview/list while preserving the summary count. No frozen web files, native dependency, schema migration, stale-row cleanup, generated files, tajweed, recite matcher, or SM-2 algorithm changes. Local validation passed with `cd artifacts/api-server && npx tsc --noEmit`, `cd artifacts/noor-mobile && npx tsc --noEmit`, `npx tsc --build lib/db`, and `git diff --check`. Hardware QA: pending Mohammad iPhone test.
- 2L.0.mm Collapsible Translation/Tafseer text in ayah action sheet — Implemented and hardware-approved May 4 in implementation commit `b7fc5c9` (`b7fc5c98753a5fb4ecfba7b8a5be066942f060cb`): Reading Mushaf Translation text now clamps to 8 lines with a More/Less toggle when the loaded text is over 300 characters, Tafseer clamps to 10 lines with a More/Less toggle when loaded text is over 400 characters, and both expansion states reset on Previous/Next ayah navigation. This keeps typical action-sheet navigation visible without scrolling while still allowing full long commentary reading on demand. JS-only; no word-by-word, highlight, note, fetch logic, navigation behavior, backend, frozen web, tajweed, recite matcher, or native dependency changes. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`. Hardware QA: Mohammad approved on hardware.
- 2L.0.nn.a Test Mushaf foundation Phase 1 — Implemented May 4 in implementation commit `03ec561` (`03ec56106275d905c948b2bdb022e842eb80e1ef`): added opt-in memorization view mode `test-mushaf`, a new `MushafTestPageView` image pager using the bundled Quran.com 1405H/1920px page images, horizontal page swipe, controlled page state tied to the current memorization ayah page, and invisible normalized per-word Pressable overlays as scaffolding only. Existing `ayah` and `page` memorization renderers and the Reading Mushaf route were not modified behaviorally. No tap-to-seek, audio sync, recite, translation popovers, backend/API/schema/shared business logic, or memorization-state visualization were added. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`; staged whitespace check covered the new component. Hardware QA: pending Mohammad iPhone smoke. Backlog: 1b tap-to-seek audio, 1c audio word highlighting, 1d recite mode, 1e translation popovers, then further feature ports one slice at a time.

---

## ⏭ Phase 2L / Phase 3 — TestFlight readiness after 2J/2K

Phase 2I memorization parity completion, Phase 2J.1-2J.3 review parity, Phase 2K Full Quran/Mushaf Bayaan parity, Phase 2L.1 visual polish, and 2L.0.mm Reading Mushaf collapsible Translation/Tafseer text are hardware-tested. Slice 2L.0.nn.a Test Mushaf foundation is locally validated with hardware QA pending. Phase 2L.0 UI refinement + backend QA pass is now active and runs before Phase 2L.2 app shell/assets. Sequence: complete Phase 2L.0 (open-ended, gated on Mohammad's sign-off), then Phase 2L.2 app shell/assets, then Phase 2L.3 production TestFlight build/QA.

- [ ] App icon, splash screen, launch screen
- [ ] Production API/env review
- [ ] EAS Build production profile → first `.ipa`
- [ ] App Store Connect setup
- [ ] TestFlight beta with wife + trusted users
- [ ] Bug fixes from beta feedback
- [ ] App Store submission
- [ ] Push notifications for review reminders (Expo Notifications) — requires explicit native/build approval if new native config is needed
- [ ] Native gestures + haptics only if already covered by installed dependencies or explicitly approved
- [x] Make audio controls in `review-session` sticky / floating at bottom of screen (Phase 2H.4 complete and hardware-tested)
- [ ] Add ayah-bounding-box overlay in `review-session`
- [ ] Migrate from `expo-av` to `expo-audio`/`expo-video` later; do not block parity on this

---

## ✅ Hardware bugs — found Apr 29, 2026 and cleared during Phase 2I.1 QA

These were reported during the Apr 29 web→mobile parity audit / hardware QA cycle. All three are now hardware-tested/shipped by Mohammad.

- **Memorize `Next up` card → loading death spiral.** Hardware-confirmed fixed by Mohammad. Temporary diagnostic logs were removed from `artifacts/noor-mobile/app/child/[childId]/memorization.tsx`; the cleanup made no behavior changes. JS-only. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- **Memorize surah search keyboard sticks open.** Implemented Apr 29 in `artifacts/noor-mobile/app/child/[childId]/memorization.tsx` and hardware-tested/shipped by Mohammad after commit `6a9e70f`: the discovery `ScrollView` uses `keyboardDismissMode="on-drag"` and `keyboardShouldPersistTaps="handled"`, the horizontal filter scroller also preserves handled taps, and the search `TextInput` uses an explicit Done return key that dismisses the keyboard. JS-only. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.
- **Needs Work → Practice same-target loader.** Hardware-confirmed fixed by Mohammad after commit `a94c541`. Implemented Apr 29 in `artifacts/noor-mobile/app/child/[childId]/memorization.tsx`: every explicit `beginSession` bumps a `sessionLoadId`, and the verse/timing fetch effect now depends on that ID and only runs when `sessionRequested` is true. This fixes the circle of death when a just-saved Needs Work range is practiced again with the same surah/range values. JS-only. Local validation passed with `cd artifacts/noor-mobile && npx tsc --noEmit` and `git diff --check`.

---

## 🟡 Backlog

- **Tajweed coloring not rendering (Slice 5a Session 2)** — wiring is in place (`src/lib/tajweed.ts`, fetcher fields, toggle, both renderers) but no colors appear on hardware. Most likely Quran.com v4 doesn't expose `text_uthmani_tajweed` as a `word_field` — only as a verse-level field. Web app uses `splitTajweedIntoWords` to parse the verse-level HTML and split into per-word HTML chunks. Mobile fix path: (1) add `fields=text_uthmani_tajweed` (verse-level) to fetcher, (2) port `splitTajweedIntoWords` from `noor-path/src/components/mushaf/bayaan/bayaan-utils.ts` to mobile, (3) at fetch time, parse each verse's tajweed HTML into a `Map<wordIdx, color>` keyed by display index, (4) consult that map in the renderers instead of `word.text_uthmani_tajweed`. Lower priority — kid can memorize fine without it.
- **DB page-number sync run pending** — Slice 2L.0.x added `scripts/sync-db-page-numbers.mjs` to push corrected QUL page numbers into Neon `quran_verses.page_number`. Mohammad should run `DATABASE_URL='<prod URL>' node scripts/sync-db-page-numbers.mjs` once, restart/redeploy the Railway API so `versePageCache` reloads, then rerun `DATABASE_URL='<prod URL>' node scripts/verify-db-page-numbers.mjs`.
- **2L.0.jj.c retire King Fahd fallback files** — After ~3 days of hardware confidence, remove `king-fahd-page-images.ts`, `king-fahd-ayah-coords.ts`, old `kfgqpc-hafs` PNGs, `aya.json`, and `specs.json`. Expected net bundle delta after cleanup: about +73 MiB versus the old ~40 MiB King Fahd bundle.
- **2L.0.kk contentFit tuning** — Current QuranEngine 1920x3106 pages intentionally use `contentFit="contain"` and letterbox on tall iPhones. Tune page sizing later, likely smarter scaling or selective `cover`, without distorting page geometry or breaking tap overlays.
- **2L.0.ll custom decorative skinning** — Follow-up only after the image-source swap settles: dark-mode parchment, ornate borders, theme treatment, and visual customization around the printed page without recoloring the source images in this slice.
- **Future review-session QuranEngine renderer** — Consider applying the same Quran.com 1405H image renderer and per-ayah overlay approach to `review-session.tsx` after Reading Mushaf settles, preserving existing review audio, queue, rating, and SM-2 behavior.
- **Persistence bug** — Slice 5a Session 1 split profile vs session settings, but profile-level still doesn't persist across app close. Likely a hydrate-effect ordering issue or `settingsLoaded` gate timing problem in `memorization.tsx`. Low priority per user.
- **Long-verse fractional lag** — `LEAD_MS = 500` works for short ayahs but trailing reappears on long verses (Al-Baqarah, Ayat al-Kursi). Root cause is equal-time-per-word approximation, not leading silence. Real fix requires per-word audio durations (Quran.com `wbw` endpoint or similar) — heavier work. Accepted limitation for now.
- Update `noor-path/src/lib/auth-client.ts` to support env-switchable baseURL
- Add Expo dev URL to `PROD_TRUSTED_ORIGINS` and `PROD_ALLOWED_ORIGINS` if RN dev needs LAN access
- Retire `feature/main-working-branch` once `main` becomes permanent working branch
- Delete `~/Desktop/skia-quran-test/` (no longer needed)
- Dashboard kid name shows just first letter when truncated (cosmetic, low priority)
- Investigate continuous-listening latency on slow networks once recite mode sees more real-world use
- Web-app parity deep dive completed Apr 28, 2026; refreshed Apr 29 with the 2I.2/2I.3/2I.4 sub-rows. Follow the Phase 2G-2K roadmap above before resuming Phase 3/TestFlight.

---

## 📚 Knowledge banked

### From infra session
- pnpm 10's `pnpm-workspace.yaml` overrides syntax doesn't work with pnpm 9. Use root `package.json`'s `pnpm.overrides` for compat.
- Railway/Railpack reads `packageManager` field for Corepack activation but ignores it for version selection.
- Railway's "Wait for CI" toggle silently blocks all webhook-triggered deploys when no GitHub Actions are configured.
- Railway's "Redeploy" replays a row's specific commit + config, NOT branch HEAD.
- Native fetch from RN sends no `Origin` header — existing CORS `if (!origin)` branch handles it.

### From rendering test
- RN's `<Text>` shapes Arabic correctly via the OS text engine when given a real Unicode font (Amiri). No HarfBuzz/Skia needed.
- Skia's basic `<Text>` does NOT shape Arabic — renders isolated glyphs LTR.
- Reanimated v4 was split: needs `react-native-worklets` as separate peer dep (added in Expo SDK 54, Sep 2025).
- Expo Go bundles Skia/Reanimated/Worklets for testing; production needs EAS Build (full Xcode required).

### From Phase 1 mobile app setup
- React Native has no `document.cookie`. `credentials: "include"` in `fetch` does nothing. Better Auth Expo pattern: `authClient.getCookie()` returns the cookie string from SecureStore, which must be manually added as `Cookie` header. With manual `Cookie` header, set `credentials: "omit"` (not "include") to avoid conflicts.
- `npx expo install` in a pnpm monorepo will run `pnpm add` at the workspace root, which can corrupt nested `node_modules` if there's also an `npm install` `package-lock.json` present. Fix: commit fully to pnpm.
- `@better-auth/expo@1.6.9` requires `expo-network` as a peer dependency. Not installed automatically.
- pnpm's `node_modules/.pnpm/` content-addressed store works fine with Metro/Expo without `node-linker=hoisted`.
- App-wide light mode: set `"userInterfaceStyle": "light"` in `app.json` AND use explicit colors in StyleSheet.

### From QPC font test (2026-04-27)
- QPC V1/V2 per-page fonts use Private Use Area codepoints, not real Unicode. Each word on a Mushaf page is encoded as a custom glyph at a PUA codepoint.
- Decision: NOT pursuing QPC font rendering. Sticking with PNG page images for Full Mushaf + Review.

### From page-image streaming test (2026-04-27)
- Streaming Mushaf pages from `raw.githubusercontent.com/GovarJabbar/Quran-PNG` works on iOS via `<Image>` + `<FlatList>`.
- URL: `https://raw.githubusercontent.com/GovarJabbar/Quran-PNG/master/{NNN}.png` (NNN zero-padded 001–604).
- Page aspect ratio ~1.45:1.
- For RTL navigation use FlatList with `inverted` prop.

### Metro + pnpm requires explicit config (2026-04-27)
- When adding native dependencies, Metro with pnpm-style symlinks fails to resolve packages by default.
- Fix: `artifacts/noor-mobile/metro.config.js` must set `config.watchFolders = [workspaceRoot]` and `config.resolver.nodeModulesPaths = [projectRoot/node_modules, workspaceRoot/node_modules]`.
- DO NOT set `disableHierarchicalLookup: true` — pnpm's deeply nested `.pnpm/` requires hierarchical lookup ON.
- After modifying `metro.config.js`, restart with `npx expo start --clear`.

### Memorization screen architecture (2026-04-27, Phase 2D + Slice 5a)
- One file: `app/child/[childId]/memorization.tsx`. ~1500 lines after Slice 5a Session 2.
- Audio state lives in refs because `expo-av` status callback closes over stale state. Pattern: `const fooRef = useRef(foo); useEffect(() => { fooRef.current = foo; }, [foo]);`
- Audio refs as of Slice 5a Session 2: `viewModeRef`, `currentVerseRef`, `ayahEndRef`, `isPlayingRef`, `isLoadingRef`, `pendingSeekPositionRef`, `repeatCountRef`, `autoAdvanceDelayRef`, `autoplayThroughRangeRef`, `reciteModeRef`, `reciteExpectedIdxRef`, `displayWordsMapRef`, `surahNumberRef`, `matchedWordCountRef`, `lastMatchedWordRef`, `lastMatchTimeRef`, `reciterRef`, `saveTimerRef`, `playbackRateRef`. Plus timer/raf/sound refs.
- Two render modes (`viewMode: "ayah" | "page"`). Two highlight states (`highlightedWord` for ayah, `highlightedPage: { verseKey, position }` for page).
- QDC segments are 1-indexed and skip non-word tokens (filter `char_type_name === "word"` first).
- Husary `qdcId: 6`, `quranComId: null`. Don't assume null `quranComId` means no timing data.
- iOS audio session conflict: `expo-av` and `expo-speech-recognition` can't both hold the session. `pauseAsync()` not enough — must `unloadAsync()`. `stopAudioCompletely()` is the single cleanup point.

### Reciter audio infrastructure (2026-04-27, Slice 5a Session 1)
- 7 reciters in `src/lib/reciters.ts`: husary, afasy, sudais, basit, minshawi, ghamdi, ajmi.
- everyayah folder names verified against the canonical `everyayah.com/recitations_pages.html` list. Three names in the original web table were stale: Sudais (`Sudais_192kbps` → `Abdurrahmaan_As-Sudais_192kbps`), Minshawi (`Minshawi_Murattal_128kbps` → `Minshawy_Murattal_128kbps` with y), Ajmi (`ahmed_ibn_ali_al-ajmy128kbps` → `Ahmed_ibn_Ali_al-Ajamy_128kbps_ketaballah.net`).
- Quran.com v4 `by_ayah` endpoint returns `segments: undefined` for all 6 non-Husary reciter IDs (verified via diagnostic logging Apr 27). Web app uses same endpoint and gets same empty result — falls back to fractional approximation.
- Fractional fallback in `tick()`: `Math.floor(shiftedFrac * wordCount)` where `shiftedFrac = (pos + LEAD_MS) / dur, clamped to 1`. `LEAD_MS = 500` after tuning. Provides anticipatory feel similar to Husary's QDC segment 1 starting at frac=0.
- iOS audio session: `Audio.setAudioModeAsync({ playsInSilentModeIOS: true })` mandatory — without it the silent switch silences playback through the speaker. AirPods always work because Bluetooth bypasses the switch.
- everyayah recordings have wildly different mastered volumes. Afasy_128kbps is significantly quieter than Husary_128kbps. Compensate with `setVolumeAsync(1.0)` on every Sound after `createAsync`.
- `setRateAsync(rate, /* shouldCorrectPitch */ true)` works on `expo-av` Sound. Range 0.75–1.5 is comfortable. `shouldCorrectPitch=true` keeps recitation pitch natural.

### Profile vs session settings (2026-04-27, Slice 5a Session 1)
- `src/lib/settings.ts` exports `loadProfileSettings`/`saveProfileSettings` (themeKey, reciterId, viewMode — persisted to AsyncStorage) and `DEFAULT_SESSION_SETTINGS` constants (repeatCount, autoAdvanceDelayMs, autoplayThroughRange, blurMode, blindMode — reset each session).
- Profile-level edited via future Profile Settings page (Phase 2E). For now, defaults hardcoded.
- Session 2 added `playbackRate` and `tajweedEnabled` as plain inline `useState` defaults (not added to `DEFAULT_SESSION_SETTINGS`); they reset on screen mount.
- Persistence currently buggy — profile settings don't survive app close. Likely hydrate-effect ordering. Low priority per user.

### Tajweed (2026-04-27, Slice 5a Session 2 — partial)
- `src/lib/tajweed.ts` has the 21-class `TAJWEED_COLORS` map (ported from `noor-path/src/components/mushaf/bayaan/bayaan-constants.ts` `TAJWEED_CSS`) plus `extractTajweedColor(html)` which extracts the first `class="..."` and looks up the hex.
- Mobile assumed Quran.com v4 supports `text_uthmani_tajweed` as a `word_field`. On hardware no colors render. Likely the field isn't populated at the word level — only at verse level (web app fetches `text_uthmani_tajweed` on the verse and uses `splitTajweedIntoWords` from `bayaan-utils.ts` to chunk it). Fix path documented in backlog.

### Translation popup (2026-04-27, Slice 5a Session 2)
- Quran.com v4 `word_fields=translation` + `translations=131` (Sahih International) populates a `translation` field on each word. May come back as a `{ text, language_name }` object OR a plain string — handle both.
- `onLongPress` with `delayLongPress={400}` is a comfortable threshold (short tap still fires `onPress`).
- RN backdrop-dismiss pattern: outer `Pressable` is the backdrop with `onPress={close}`; inner card is also a `Pressable` with `onPress={() => {}}` (empty) to absorb taps so they don't bubble to the backdrop. Plain `<View>` for the card would let taps fall through.

### Arabic fuzzy matching for on-device speech recognition (2026-04-27, Slice 4 hotfixes)
- iOS speech recognition returns plain Arabic — no hamza variants (أإآاٱ all collapse to ا), no ta-marbuta (ة → ه), no ya-with-hamza (ئ → ي), often without "ال" prefix.
- The web app's `stripTashkeel` does the full normalization.
- Multi-predicate match: equality, substring either direction, subsequence either direction, noun-vowel-stripped equality, word-final ت→ه swap. `ال` prefix stripping with ≥2-char fallback. 1-char heard tokens reject unless equal.
- iOS sends growing partial transcripts each event. Matcher walks full transcript from `matchedWordCountRef` forward.
- iOS recognition has 60s session limit + warm-up gibberish at restart. Multi-predicate matchers reject the gibberish naturally.

### EAS dev build (2026-04-27)
- Expo Go does not include native modules. Use `eas build --profile development --platform ios`.
- Use `npx eas-cli@latest <cmd>` (avoid global-install permission issues).
- iOS 16+ requires Developer Mode (Settings → Privacy & Security → Developer Mode → enable → restart).
- Trust developer profile in iPhone Settings → General → VPN & Device Management.
- Run dev server: `npx expo start --dev-client` (the `--dev-client` flag is critical).
- Re-run `eas build` only when adding new native deps or changing `app.json` plugin entries. Pure JS hot-reloads.

---

## 🔐 Environment / URLs reference

| Thing | Value |
|---|---|
| Backend (prod) | `https://workspaceapi-server-production-cc25.up.railway.app` |
| Health check | `https://workspaceapi-server-production-cc25.up.railway.app/api/healthz` |
| Backend (dev) | `http://localhost:3001` |
| Frontend (dev) | `http://localhost:5173` |
| Database | Neon serverless Postgres (rotated Apr 26) |
| Repo | `https://github.com/mothman0406/Quranic-Journey` |
| Branches | `main` (deploy) + `feature/main-working-branch`; at Slice 2L.0.nn.c docs commit, `main`, `origin/main`, `feature/main-working-branch`, and `origin/feature/main-working-branch` are all synced to the final Slice 2L.0.nn.c docs HEAD. `safe-cumulative` was temporary archaeology and can be ignored. |
| Apple Developer | Approved Apr 26; Team ID `M7KJJDN537` |
| iOS bundle identifier | `com.mothman.noorpath` |
| EAS project | `@mothman123/noor-mobile` |
| Railway project | `humble-laughter` / `production` env |
| Mobile app HEAD | Phase 2D complete through Slice 5b, Phase 2E dashboard polish complete, Phase 2F targets complete, Phase 2G.1-2G.3 hardware-tested, Phase 2H.1-2H.6 hardware-tested, Phase 2I.1 memorization overview cards plus save-semantics/rating follow-up hardware-tested, Phase 2I.2a Pause & Save hardware-tested, Phase 2I.2b Save & Leave hardware-tested, Phase 2I.2c Recitation Check hardware-tested, Phase 2I.2d Ready to Recite hardware-tested, Phase 2I.2e Just Get Tested hardware-tested, Phase 2I.2f Pre-session setup hardware-tested, 2I.2g Bookmark / resume hardware-tested, 2I.2h View in Full Mushaf hardware-tested, 2I.2i Skip repeat / Skip ayah hardware-tested, 2I.2j Tappable ayah end-marker hardware-tested, 2I.2k Tap-tooltip word translation + WBW audio hardware-tested, 2I.2l Recite-mode page auto-flip hardware-tested, 2I.2m Show Word / Skip Word plus real blur hardware-tested, 2I.2n multi-page recite navigation pills hardware-tested, 2I.2o confetti / celebration overlay hardware-tested, 2I.3 memorization settings/defaults hardware-tested, 2I.4 discovery polish hardware-tested, Phase 2J.1 review completed-today/continue-next hardware-tested, Phase 2J.2 ahead-day local session behavior hardware-tested, Phase 2J.3 Review Multiple hardware-tested, Phase 2K.1 Mushaf tool shell hardware-tested, Phase 2K.2 ayah sheet and annotations hardware-tested, Phase 2K.3 audio range / recite / select-to-memorize hardware-tested, Phase 2L.1 visual polish hardware-tested, 2L.0.jj Quran.com 1405H/1920px Reading Mushaf hardware-tested, 2L.0.ll Reading Mushaf UX polish hardware-tested, 2L.0.mm collapsible Translation/Tafseer action-sheet text hardware-tested, 2L.0.nn.a Test Mushaf foundation locally validated with hardware QA pending, 2L.0.nn.b Test Mushaf tap-flash debug feedback committed at `a22af07`, 2L.0.nn.c Test Mushaf audio word highlighting committed at `ac56040` with hardware QA pending, `Next up` skip-gap behavior hardware-tested, and Today's/Current work freeze correction hardware-tested. Backend cumulative recitation scheduling was corrected during Phase 2I.1 QA. EAS dev build `cfb3f406-5fec-405a-a150-e525a96ecff2` finished and was hardware-tested by Mohammad for earlier slices. Beta is not done; Phase 2L.0 UI refinement remains active with the previous backlog reminders unchanged before Mohammad decides 2L.0 is done and 2L.2 app shell/assets can start. |
</content>
